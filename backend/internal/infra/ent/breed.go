// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/artesipov-alt/odnoi-krovi-app/internal/infra/ent/breed"
)

// Breed is the model entity for the Breed schema.
type Breed struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id"`
	// Name holds the value of the "name" field.
	Name string `json:"name"`
	// Type holds the value of the "type" field.
	Type breed.Type `json:"type"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BreedQuery when eager-loading is set.
	Edges        BreedEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BreedEdges holds the relations/edges for other nodes in the graph.
type BreedEdges struct {
	// Pets holds the value of the pets edge.
	Pets []*Pet `json:"pets,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedPets map[string][]*Pet
}

// PetsOrErr returns the Pets value or an error if the edge
// was not loaded in eager-loading.
func (e BreedEdges) PetsOrErr() ([]*Pet, error) {
	if e.loadedTypes[0] {
		return e.Pets, nil
	}
	return nil, &NotLoadedError{edge: "pets"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Breed) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case breed.FieldID, breed.FieldName, breed.FieldType:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Breed fields.
func (_m *Breed) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case breed.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case breed.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case breed.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				_m.Type = breed.Type(value.String)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Breed.
// This includes values selected through modifiers, order, etc.
func (_m *Breed) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryPets queries the "pets" edge of the Breed entity.
func (_m *Breed) QueryPets() *PetQuery {
	return NewBreedClient(_m.config).QueryPets(_m)
}

// Update returns a builder for updating this Breed.
// Note that you need to call Breed.Unwrap() before calling this method if this Breed
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Breed) Update() *BreedUpdateOne {
	return NewBreedClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Breed entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Breed) Unwrap() *Breed {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Breed is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Breed) String() string {
	var builder strings.Builder
	builder.WriteString("Breed(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", _m.Type))
	builder.WriteByte(')')
	return builder.String()
}

// NamedPets returns the Pets named value or an error if the edge was not
// loaded in eager-loading with this name.
func (_m *Breed) NamedPets(name string) ([]*Pet, error) {
	if _m.Edges.namedPets == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := _m.Edges.namedPets[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (_m *Breed) appendNamedPets(name string, edges ...*Pet) {
	if _m.Edges.namedPets == nil {
		_m.Edges.namedPets = make(map[string][]*Pet)
	}
	if len(edges) == 0 {
		_m.Edges.namedPets[name] = []*Pet{}
	} else {
		_m.Edges.namedPets[name] = append(_m.Edges.namedPets[name], edges...)
	}
}

// Breeds is a parsable slice of Breed.
type Breeds []*Breed
