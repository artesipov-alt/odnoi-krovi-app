# Миграции базы данных

Каталог содержит SQL-миграции схемы и данных. Применяются вручную через
Taskfile (`task db:migrate`) или UI-клиент БД (Beetkeeper) на каждую базу
отдельно (dev, prod, local).

## Schema vs Data migrations vs Seeds

В проекте разделены три слоя работы с БД (см. [backend/AGENTS.md](../AGENTS.md)):

| Слой | Что делает | Когда применяется | Инструмент |
|---|---|---|---|
| **Schema migrations** | DDL: таблицы, индексы, колонки | При каждом старте приложения (auto-migrate) | Ent `client.Schema.Create` в `config.RunMigrations` |
| **Data migrations (SQL)** | Одноразовое преобразование данных + стабильные справочники | Один раз на окружении | SQL-файлы в этом каталоге, применяются через `task db:migrate` |
| **Seeds** | Начальное наполнение справочников на пустой БД | Не используются — заменены SQL-миграциями | — |

**Почему нет seeds в коде.** Ранее справочники (`ref_locations`, `ref_breeds`)
наполнялись через Go-пакет `pkg/seeds` при старте приложения. Это создало две
проблемы: (1) auto-seed при каждом старте — риск на каждом деплое и скрытие
ошибок; (2) дублирование источника правды между кодом и БД. Вместо этого
стабильные справочники живут как idempotent SQL-миграции — применяются один
раз на окружение, версионированы в git, не перезаписывают данные при повторном
запуске.

## Конвенция именования

```
YYYYMMDDNNNNNN_name.sql
```

- `YYYYMMDD` — дата (год-месяц-день)
- `NNNNNN` — порядковый номер в пределах дня (6 цифр, для сортировки)
- `name` — короткое описание в snake_case

Пример: `20260728000001_regions_okato.sql`

## Правила

1. **Idempotent.** Каждая миграция должна быть безопасна для повторного
   запуска: `ON CONFLICT DO NOTHING`, `IF NOT EXISTS`, `CASE`-обновления.
   Это упрощает применение на нескольких окружениях и восстановление после
   сбоев.

2. **Транзакция.** Оборачивайте связанную логику в `BEGIN/COMMIT`. При ошибке
   всё откатится. Исключение — операции, которые нельзя выполнить в
   транзакции (например, `CREATE INDEX CONCURRENTLY`).

3. **Порядок внутри миграции с переименованием ID** (для справочников):
   1. Вставка новых записей (`ON CONFLICT DO NOTHING`).
   2. Обновление ссылок в JSON-полях (`jsonb_agg` + `CASE`).
   3. Обновление FK-колонок.
   4. Удаление старых записей справочника (после обновления всех ссылок).

4. **Schema vs Data.** Schema-миграции (DDL) также выполняются автоматически
   через `config.RunMigrations` (Ent `client.Schema.Create`) при старте
   приложения. SQL-миграции в этом каталоге — для данных (справочники) и
   для DDL, который Ent не умеет выразить (partial indexes, сложные
   ограничения и т.п.).

5. **Порядок применения на окружениях**: dev → проверка → prod. Перед prod —
   бэкап.

## Применение

### Через Taskfile (рекомендуемый способ)

```sh
# Применить все миграции к локальной БД (по умолчанию ENV=local)
task db:migrate

# Применить к dev
ENV=dev task db:migrate

# Применить к prod (после бэкапа!)
ENV=prod task db:migrate

# Только регионы или только породы
task db:migrate:regions
task db:migrate:breeds
```

Taskfile использует утилиту `backend/cmd/dburl` для получения DSN из той же
конфигурации (`config.NewEntConfig`), что и приложение — никакого дублирования.

### Через psql напрямую

```sh
psql "$(go run ./cmd/dburl dev)" \
  -v ON_ERROR_STOP=1 \
  -f migrations/20260728000001_regions_okato.sql
```

### Через Beetkeeper

Открыть SQL-редактор, выбрать целевую БД, вставить содержимое файла,
выполнить целиком.

## Процесс для нового разработчика

1. Клонировать репозиторий.
2. Поднять локальный Postgres (см. `config.NewLocalConfig` — `localhost:5432`,
   БД `local_odnoi_krovi`, пользователь `admin`).
3. Запустить backend один раз — `task dev-backend` применит schema-migrations
   (Ent auto-migrate создаст таблицы).
4. Применить справочники — `task db:migrate` (вставит 89 регионов и 418 пород).
5. Можно работать.

## Текущие миграции

| Файл | Назначение |
|---|---|
| `20260728000001_regions_okato.sql` | Переход на коды ОКАТО субъектов РФ (89 регионов); замена старых ID `MSK`→`77`, `MO`→`50` во всех ссылках (ref_locations, blood_requests.regions, donor_preferences.preferred_location_ids, users.location_id). |
| `20260728000002_breeds.sql` | Полный справочник пород (ref_breeds) для кошек и собак. |