# Журнал изменений

Все значительные изменения в этом проекте будут документированы в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
и проект следует [Семантическому Версионированию](https://semver.org/lang/ru/).


## [3.20.3] - 2026-07-08

### Изменено

- **Детали питомца в уведомление `recipient_empty_showcase`.**
  SQL-запрос `queryRecipientEmptyShowcase24h` теперь выбирает имя питомца, группу крови и объём. Payload уведомления содержит поля `petName`, `bloodGroup`, `volume`.
  Затронутые файлы: `internal/infra/scheduler/job/notification_queries.go`, `internal/infra/scheduler/job/notification_job.go`.

## [3.20.2] - 2026-07-05

### Добавлено

- **`DonorMatchNotifier`:** Добавлен сервис `DonorMatchNotifier` (`internal/application/bloodsearch/service/`) для генерации уведомлений при поиске доноров крови после создания заявки.
- **`PetService`:** Введен интерфейс `PetService` для абстрагирования бизнес-логики питомцев. Старая реализация переименована в `PetServiceV1`, создана новая `PetServiceV2` с чистыми функциями.
- **Новые методы `PetReadRepository`:** Добавлены `GetPetIDsByBloodGroupAndRegion` (raw SQL для быстрого поиска ID) и `GetByIDs` (пакетная загрузка питомцев по ID).
- **Хелперы в модели питомца:** Добавлены хелперы `HasStopFactors` и `IsRecovering` для более читаемой проверки статуса.

### Изменено

- **Порядок параметров:** В `GetStopFactors`/`RecalculateFactors` порядок параметров изменен: `isRecipient` теперь передаётся до `isPlaningDonation` для единообразия.

### Исправлено

- **Nil pointer dereference:** Исправлена ошибка nil pointer dereference в `RecalculateFactorsAndStatus` при вызове `bloodReq.IsClosed()` через promoted method на nil-`*BloodRequestWithApplications`. Добавлены nil-guards (`bloodReq != nil && !bloodReq.IsClosed()`) на стороне вызывающего кода в `PetServiceV1` и `PetServiceV2`. Аналогично защищены вызовы `application.IsActiveForDonation()`.

## [3.20.0] - 2026-07-03

### Изменено

- **Рефакторинг EventPublisher: 9 каналов → 1 канал `events`:**
  - Интерфейс `EventPublisher` сокращён до двух методов: `PublishEvent` и `PublishNotification`.
  - Добавлены `EventType` и `EventEnvelope` — тип события теперь передаётся внутри envelope, а не через имя канала.
  - Удалены 9 констант каналов Redis, 9 методов `PublishXxx` и 9 заглушек `NoOpEventPublisher`.
  - Все command handlers переведены на `PublishEvent(ctx, EventType, payload)`.
  - Структура `BloodRequestCreated` упрощена: убраны `[]Peers`, добавлены плоские поля `TelegramID`/`MaxID`. В `create_req.go` — цикл с отдельным `PublishEvent` на каждого донора.
  - Добавлены JSON-теги во все event-структуры для консистентной camelCase-сериализации.
  - Удалены неиспользуемые методы `EventName()` и `OccurredAt()` из всех event-структур.

### Технические детали

- **Боты:** tg-bot и max-bot переведены на единый канал `events` с диспатчем по `EventEnvelope.type`. Все handler'ы обновлены на camelCase-поля. `handleBloodRequestCreated` переписан под одного донора (без цикла по `AvilableDonors`).

## [3.19.4] - 2026-07-02

### Исправлено
- **Перенос питомцев при слиянии аккаунтов:**
  - При слиянии аккаунтов через верификацию телефона питомцы теперь переносятся к существующему пользователю, а не остаются сиротами (без владельца).
  - Добавлен метод `TransferPets` в репозиторий.
- **Защита от слияния аккаунтов с одинаковым провайдером:**
  - Добавлена проверка `HasProviderConflict` на доменной модели `User` — если у объединяемых пользователей есть пересечение по провайдерам (`telegram_bot`, `max_bot`), слияние блокируется с ошибкой `409 Conflict`.
  - Это предотвращает чёрное донорство, когда пользователь пытается объединить аккаунт из Telegram с другим Telegram-аккаунтом.


## [3.19.2] - 2026-07-02

### Добавлено
- **Уведомления: Добавлен флоу автоматического закрытия запроса при неактивности получателя в течение 12 часов:**
  - **Бэкенд:**
    - Добавлен `queryRecipientInactive12h` для поиска активных запросов с ожидающими ответами доноров, где получатель не был замечен в течение 12 часов.
    - Реализован `checkRecipientInactive12h`: закрывает запрос крови через `CloseRequestHandler` и публикует `NotifRecipientSearchClosed`.
  - **Боты (tg-bot и max-bot):**
    - Добавлен шаблон сообщения `recipient_search_closed` с текстом из спецификации.
- **Уведомления: Подписка tg-bot и max-bot на канал уведомлений:**
  - Добавлен обработчик `handleNotification` в tg-bot и max-bot, который прослушивает канал Redis `{env:}notifications`.
  - Поддержка 3 типов уведомлений: `recipient_donor_waiting`, `recipient_inactive_warning`, `donor_not_accepted`.
  - Использование `camelCase` для ключей `Payload` для соответствия соглашению JSON.
  - Обновлены версии ботов: tg-bot до v2.2.0, max-bot до v0.7.0.
  - Обновлены `CHANGELOG` для обоих ботов.



## [3.19.1] - 2026-07-01

### Изменено
- **Рефакторинг (bloodsearch): Использование динамической причины для отклонения донации:**
  - Обновлена логика отклонения донации для использования динамических причин, что позволяет более гибко управлять отказами и предоставлять более точную информацию. Это изменение улучшает обработку отклонений, позволяя системе предоставлять конкретные причины, а не общие сообщения об ошибках.

## [3.19.0] - 2026-06-29

### Добавлено
- **Добавлено поле `last_seen_at` и метод `UpdateLastSeen`:**
  - Добавлено поле `last_seen_at` в схему Ent (опциональное, обнуляемое время).
  - Добавлено поле `LastSeenAt` в доменную модель `User`.
  - Добавлен метод `UpdateLastSeen(ctx, id) error` в интерфейс репозитория.
  - Добавлена реализация `UpdateLastSeen` в репозитории на основе Ent.
  - Добавлено сопоставление `LastSeenAt` в маппере Ent-to-domain.
  - Добавлено поле `lastSeenAt` в DTO `UserDetail`.
  - Добавлено сопоставление `LastSeenAt` в DTO-маппере.
- **Простановка поля `Last seen` при аутентификации:**
  - Добавлена логика для автоматической установки поля `Last seen` при аутентификации пользователя.

## [3.18.6] - 2026-06-25

### Изменено
- **Рефакторинг `DonorResponse`:**
  - Бизнес-операции `Accept`, `Reject`, `Complete`, `Confirm`, `Cancel` перенесены из репозитория в модель `DonorResponse`.
  - Репозиторий теперь отвечает только за сохранение агрегата через единый метод `Update`.
  - Добавлен метод `Cancel` в модель `DonorResponse` с валидацией статуса.
  - 6 обработчиков команд (`confirm_donation`, `reject_donation`, `accept_response`, `close_req`, `complete_donation`, `cancel_donation`) обновлены для использования нового паттерна.
- **Исправлена ошибка копирования значения в циклах `confirm_donation` и `close_req`:**
  - Заменён `for _, app := range` с `&app` на `for i := range` с указателем на оригинал элемента слайса, чтобы избежать некорректного копирования значений.

## [3.18.5] - 2026-06-24

### Исправлено
- **Уведомления отклонённым донорам и обработка ошибок публикации:**
  - Добавлена отправка событий `DonorReject` в Redis для всех доноров, автоматически отклонённых при закрытии заявки. Это включает сбор `rejectedDonorIDs` в транзакции и последующую загрузку данных каждого донора для публикации `DonorReject` в `confirm_donation.go` и `close_req.go`.
  - Обновлён вызов `NewCloseRequestHandler` в `cmd/api/main.go` для включения `petRepo` и `userRepo`.
  - Во всех обработчиках публикация уведомлений теперь не является фатальной: вместо `return err` используется `slog.Error(...)` для событий `PublishDonorApply`, `PublishDonorReject`, `PublishDonorNotConfirmed`, `PublishDonationConfirmed`, `PublishUserContact`.

### Изменено
- **Выравнивание логики условий в `close_req.go`:**
  - Развёрнутое условие `Status == Pending || Status == Accepted || (Status == Completed && !IsConfirmed)` заменено на `application.IsActiveForDonation()` для согласованности с `confirm_donation.go`.

## [3.18.4] - 2026-06-23

### Добавлено
- **Автоматическое подтверждение донаций через 72 часа:**
  - Добавлен новый пакет `scheduler` — легковесный планировщик задач, запускающий зарегистрированные job'ы с заданным интервалом в фоновых горутинах. Поддерживает graceful shutdown через контекст.
  - Реализован job `auto_confirm_job` — воркер, который каждые 10 минут ищет донорские отклики (donor responses) со статусом `completed`, но не подтверждённые (`is_confirmed = false`), у которых `updated_at` старше 72 часов. Для каждого такого отклика вызывается `ConfirmDonationHandler`.
  - Добавлен метод `FindNotConfirmed` в репозиторий — Ent-запрос с фильтрацией по `updated_at <= cutoff`, статусу `completed` и флагу `is_confirmed = false`.
  - В `main.go` добавлены инициализация job'а и планировщика, запуск воркера после инициализации всех зависимостей, но до старта HTTP-сервера.
  - Реализован улучшенный graceful shutdown: сначала останавливается scheduler, затем HTTP-сервер, затем БД и Redis. Добавлено логирование этапов остановки и обработка ошибок при закрытии БД и Redis.

## [3.18.0] - 2026-06-11

### Добавлено
- **Добавлены identity владельцев в карточки донаций:**
  - В запросы `GetDonationHandler` и `PlannedDonationsHandler` добавлена загрузка данных владельца с включением identities (`WithIdentities: true`).
  - В транспортные DTO (`PetWithApplication`, `RecipientForDonor`) добавлено поле `identities []Identity`.
  - В хендлерах `GetDonation` и `GetPlannedDonations` реализован маппинг identity провайдеров в ответ.
  - На фронтенде типы `DonationForRecipientDonorData` и `PlannedDonationRecipientInfo` дополнены полем `identities`.
  - Реализована функция `matchIdentities` для пересечения identities текущего пользователя и владельца карточки по `providerName`.
  - В компонентах `DonorForRecipient`, `DonationDetails` (для владельца и для поиска) открытие чата теперь передаёт только пересечённые identities.
  - Исправлено открытие номера телефона в Telegram-окружении: вместо `window.open(phone)` теперь создаётся и кликается `tel:`-ссылка через DOM-элемент `<a>`.
  - В `DonorForRecipient` подтягиваются данные текущего пользователя через `useGetUserById(userId)` для корректного сравнения identities.

## [3.17.6] - 2026-06-08

### Изменено
- **Рефакторинг карточек донаций: Добавлена контактная информация владельца в ответы на донации:**
  - В ответы на донации добавлена контактная информация владельца, что улучшает коммуникацию и координацию между донорами и получателями.

## [3.17.4] - 2026-06-04

### Добавлено
- **Добавлено поле `verified` в структуры пользователя:**
  - Добавлено новое булево поле `verified` в модель `User`.
  - В схему Ent добавлено поле `verified`.
  - Обновлены DTO пользователя для включения поля `verified`.



## [3.17.3] - 2026-06-04

### Изменено
- **Уменьшено время ожидания повторного набора номера OTP без ответа до 20 секунд:**
  - Время ожидания повторного набора номера OTP без ответа было уменьшено с 60 до 20 секунд. Это изменение направлено на ускорение процесса аутентификации и уменьшение задержек для пользователей.



## [3.17.2] - 2026-06-04

### Исправлено
- **Перемещена константа, обновлен тест, исправлена аутентификация, отключен жизненный цикл OTP:**
  - Перемещены модели
  - Обновлен тест `TestTwin24OTPSender_SendOTP` для использования заглушки `http.Client`.
  - Исправлена аутентификация `Twin24`.
  - Отключен жизненный цикл задания.



## [3.17.1] - 2026-06-03

### Исправлено
- **Повторное использование ежедневной задачи и пропуск ручного запуска в Twin24 OTP:**
  - Добавлено повторное использование ежедневной задачи с поддержкой Redis в отправителе OTP Twin24, чтобы избежать создания новой задачи для каждого запроса OTP.
  - Удален ручной запуск задачи, теперь используется `ForceStart` в пакете кандидатов.


## [3.17.0] - 2026-06-03

### Добавлено
- **Реализована отправка OTP через Twin24 с использованием токена обновления:**
  - Добавлено кэширование токена обновления и процесс обновления API.
  - Рефакторинг клиента для поддержки аутентификации носителя.
  - Реализован полный жизненный цикл задачи OTP: создание, добавление кандидатов, запуск, пауза, остановка.
  - Обновлены тесты.

### Изменено
- **Интеграция отправителя OTP Twin24 в обработчик смены телефона:**
  - Учетные данные Twin24 считываются из переменных окружения.
  - Отправка OTP корректно отключается, если не полностью настроена.
  - Повторно включена фактическая генерация кода OTP.




## [3.16.1] - 2026-06-01

### Исправлено
- **Обработка ошибки нарушения ограничения при обновлении пользователя:**
  - Реализована корректная обработка ошибки нарушения ограничения при обновлении пользователя, что предотвращает сбои и обеспечивает стабильность работы приложения.



## [3.16.0] - 2026-06-01

### Добавлено
- **Реализован процесс смены телефона на основе OTP с объединением аккаунтов:**
  - Реализованы `ChangePhoneHandler` и `VerifyPhoneHandler` с верификацией по OTP.
  - Добавлен репозиторий OTP Redis с поддержкой TTL и запасным вариантом NoOp.
  - Добавлен порт `OTPSender` для интеграции SMS.
  - Расширен репозиторий пользователей с помощью `UpdatePhone`, `TransferUserIdentity`, `TransferUTMHistory`, `DeleteDonorPreferenceByUserID`, `DeleteUserHard`.
  - Устарело поле `phone` в `UpdateUserBody`; добавлены DTO `ChangeUserPhoneBody` и `VerifyUserPhoneBody`.
  - Версия API увеличена до 3.16.0 и перегенерирован клиент TypeScript.

## [3.15.6] - 2026-04-29

### Добавлено
- **Поле `IsProfileLock` в модель питомца:**
  - Добавлено новое булево поле `IsProfileLock` в модель `Pet`. Это поле указывает, заблокирован ли профиль питомца.
  - Поле добавлено в:
    - Доменную модель `Pet`.
    - Схему Ent для хранения данных.
    - DTO для ответов API.
  - Методы `SetLastDonation` и `SetTransfused` в репозитории питомцев обновлены для установки `IsProfileLock` в `true` при наступлении этих событий.

## [3.15.5] - 2026-04-28

### Изменено
- **Рефакторинг создания запроса крови и поиска питомцев:**
  - Удален конструктор `NewBloodRequest`.
  - Обновлен маппер `FromCreate` для прямого заполнения полей `BloodRequest`.
  - Изменен `GetPetsByBloodGroupAndRegion` для приема `PetType` и использования `SearchingBloodGroupNames` для фильтрации по группе крови.
  - Обеспечена фильтрация поиска питомцев по `PetType`.


## [3.15.4] - 2026-04-28

### Изменено
- **Удален неиспользуемый обработчик событий `newRecipients`:**
  - Обработчик событий `newRecipients` больше не нужен. Этот коммит удаляет обработчик, связанный с ним импорт и сопоставление событий.
  - Также повышена версия пакета.


## [3.15.3] - 2026-04-27

### Исправлено
- **Включение деталей донора в маппер заявки:**
  - Включены детали донора в маппер заявки.
- **Обработка неизвестных групп крови донора и получателя в MaxBot:**
  - Реализована обработка неизвестных групп крови донора и получателя в MaxBot.



## [3.15.2] - 2026-04-24

### Изменено
- **Рефакторинг проверки наличия заболеваний у питомца из предупреждающих в стоп-факторы:**
  - Добавлена константа `StopFactorHasDiseases`, удалена `WarnFactorHasDiseases`.
  - Обновлена функция `checkStopHealth()` для возврата стоп-фактора, когда `HealthStatus == Ill`.
  - Обновлена функция `checkWarnAge()` для предупреждения за 3 месяца до 8 лет (а не за 1 год).
  - Синхронизированы `factorDescriptions` со спецификацией (пустое `sub_description` для стоп-факторов).



## [3.15.1] - 2026-04-24

### Добавлено
- **Функциональность приоритетного поиска в запросах крови:**
  - Добавлена возможность использовать приоритетный поиск при создании запроса крови.
  - `CreateRequestHandler` теперь принимает зависимости `bonus.Repository` и `txManager`.
  - Обработчик вычитает количество приоритетных поисков, если `PrioritySearch` включен и у питомца нет привилегии.
  - `SubtractPrioritySearch` в `EntBonusRepository` обновлен, чтобы предотвратить уменьшение счетчика ниже нуля.
  - Обработчик `AddPetToBloodRequestPool` теперь извлекает ID пользователя из контекста и присваивает его запросу крови.
- **Синхронизация привилегий и приоритетного поиска:**
  - Добавлен новый метод `SyncPrivilegeAndPriority` в модель `BloodRequestWithMatchingDonors`.
  - Этот метод гарантирует, что если у получателя есть привилегия, `PrioritySearch` устанавливается в `true`, и наоборот, если `PrioritySearch` равен `true`, привилегия получателя устанавливается в `PrivilegePrioritySearch`.
  - Тип `Privilege` был перемещен из `pet/model` в `common` для более широкого использования.
  - Метод `SyncPrivilegeAndPriority` вызывается при перечислении запросов крови и получении сведений о получателе.
- **Привилегия приоритетного поиска для питомца:**
  - Присваивается `PrivilegePrioritySearch` питомцу, если у него есть активный запрос крови с приоритетом и нет существующей привилегии.
- **Поле привилегии в модели питомца:**
  - Добавлено новое поле `Privilege` в модель питомца.
  - Это поле представляет собой перечисление, которое указывает на особые привилегии, такие как "художник", "терапевт", "бывший донор", "собака-поводырь" или "приоритетный поиск".
  - Поле `bonuses` заменено на `privilege` в схеме базы данных.

## [3.15.0] - 2026-04-23

### Добавлено
- **Реализована конечная точка `GetAssignedBonuses`:**
  - Добавлена новая конечная точка для получения бонусов, назначенных донору.
  - Добавлен обработчик запросов `GetAssignedBonuses`.
  - Добавлен метод `GetAssignedBonuses` в интерфейс репозитория бонусов.
  - Добавлены DTO `AssignedBonus` и `AssignedBonusItem` для ответа API.
  - Обновлен DTO `ImportBonusesResult` с русскими описаниями.
  - Сгенерированы новые модели клиента OpenAPI.

## [3.14.15] - 2026-04-23

### Изменено
- **Перемещен импорт бонусов в обработчик файлов:**
  - Функциональность импорта бонусов перемещена из выделенного `BonusHandler` в существующий `FileHandler`. Это объединяет операции, связанные с файлами, и устраняет необходимость в отдельном `BonusHandler`.

## [3.14.14] - 2026-04-23

### Исправлено
- **Сделаны `AvailableBonuses` необязательными:**
  - Поле `AvailableBonuses` в DTO теперь необязательное.

## [3.14.13] - 2026-04-23

### Изменено
- **Добавлено поле `AssignedAt` в модель бонуса:**
  - Добавлено поле `AssignedAt` в модель бонуса. Это поле будет хранить дату и время назначения бонуса пользователю, которое устанавливается при подтверждении.

## [3.14.12] - 2026-04-23

### Исправлено
- **Удалено поле `last_donation` из пользователя:**
  - Поле `last_donation` было удалено из модели `User` и схемы базы данных. Это поле использовалось для определения, заблокирован ли пользователь от получения бонусов, но теперь эта логика обрабатывается проверкой `len(bonuses) == 0`.
  - Функция `UnassignBonuses` переименована в `UnassignReservedBonuses`, чтобы лучше отразить ее назначение — отмену резервирования бонусов.

### Изменено
- **Рефакторинг логики блокировки бонусов:**
  - Изменен механизм блокировки бонусов. Вместо использования поля `LastDonation` система теперь проверяет поле `UpdatedAt` самого последнего бонуса пользователя, чтобы определить, заблокирован ли он от получения новых бонусов.
  - Это изменение упрощает логику, устраняя необходимость в отдельном флаге `IsLockedForBonuses` и вызове `SetLastDonation` при отмене назначения бонусов.
- **Рефакторинг логики блокировки бонусов в модель пользователя:**
  - Логика блокировки бонусов перемещена из службы бонусов в новый метод `IsLockedForBonuses` в модели пользователя. Это централизует логику и делает ее повторно используемой в различных частях приложения.
  - Также введена фабричная функция `NewLockBonus` для упрощения создания бонусов.

## [3.14.11] - 2026-04-22

### Добавлено
- **Добавлены бонусы к запланированным и завершенным донациям:**
  - Добавлена возможность получать и отображать бонусы, связанные с запланированными и завершенными донациями для доноров.
  - В конструкторы и структуры `PlannedDonationsHandler` и `CompletedDonationsHandler` добавлен `bonusRepo`.
  - `GetPlannedDonationsResult` и `GetCompletedDonationsResult` изменены для включения поля `Bonuses`.
  - Реализован `GetBonusesByDonorResponseID` в `bonus.Repository` и его Ent-реализация.
  - Обновлены HTTP-обработчики `GetPlannedDonations` и `GetCompletedDonations` для получения и маппинга бонусов в DTO ответов.
  - Поле `Bonuses` в DTO `ApplicationShort` изменено с `[]string` на `[]common.Bonus`.
  - Добавлена структура `Bonus` в пакет `common` для ответов API.

## [3.14.10] - 2026-04-22

### Добавлено
- **Добавлены бонусы к запланированным и завершенным донациям:**
  - Добавлена возможность получать и отображать бонусы, связанные с запланированными и завершенными донациями для доноров.
  - В конструкторы и структуры `PlannedDonationsHandler` и `CompletedDonationsHandler` добавлен `bonusRepo`.
  - `GetPlannedDonationsResult` и `GetCompletedDonationsResult` изменены для включения поля `Bonuses`.
  - Реализован `GetBonusesByDonorResponseID` в `bonus.Repository` и его Ent-реализация.
  - Обновлены HTTP-обработчики `GetPlannedDonations` и `GetCompletedDonations` для получения и маппинга бонусов в DTO ответов.
  - Поле `Bonuses` в DTO `ApplicationShort` изменено с `[]string` на `[]common.Bonus`.
  - Добавлена структура `Bonus` в пакет `common` для ответов API.

## [3.14.9] - 2026-04-22

### Изменено
- **Добавлен ID ответа донора к назначенным бонусам:**
  - Добавлено новое поле `donor_response_id` к сущности бонуса. Это поле будет хранить ID ответа донора, который привел к назначению бонуса.
  - Метод `AssignBonuses` в службе и репозитории бонусов обновлен для приема и сохранения этого нового ID. Это позволяет лучше отслеживать и привязывать бонусы к конкретным ответам доноров.



## [3.14.8] - 2026-04-21

### Добавлено
- **Добавлено `TotalPrioritySearch` в `GetPetsByUserResult`:**
  - Включено количество приоритетных поисков в DTO `GetPetsByUserResult` и соответствующий результат запроса приложения.

## [3.14.7] - 2026-04-21

### Изменено
- **Рефакторинг логики назначения и подтверждения бонусов:**
  - Методы `AssignBonuses` и `ConfirmBonuses` в `BonusService` теперь не получают напрямую время `lastDonation`. Вместо этого `BonusService` извлекает и устанавливает `lastDonation` через `BonusRepository`. Это централизует обработку `lastDonation` в `BonusService` и `BonusRepository`.
  - `ApplyForRequestHandler` больше не извлекает `LastDonation` пользователя-донора напрямую, что упрощает обязанности обработчика.

## [3.14.6] - 2026-04-21

### Изменено
- **Обновление подтверждения бонусов для включения даты донации:**
  - Метод `ConfirmBonuses` теперь принимает параметр `donationDate`. Эта дата используется для установки поля `last_donation` для пользователя при подтверждении бонусов.
  - Методы `AddPrioritySearch` и `SubtractPrioritySearch` перемещены из `user_repo`.

## [3.14.5] - 2026-04-21

### Изменено
- **Рефакторинг статуса бонуса для использования поля стадии:**
  - Заменено булево поле `IsActive` на поле перечисления `Stage` (`stage`, `used`, `unused`, `reserved`) для более детального управления статусом бонуса. Это влияет на создание, получение бонусов и различные обработчики, которые взаимодействуют с бонусами.

## [3.14.4] - 2026-04-21

### Добавлено
- **Добавлена логика агрегации бонусов и отслеживание донаций пользователя:**
  - Введена новая служба агрегации бонусов, которая предоставляет один бонус на подкатегорию для каждого партнера, приоритизируя по дате истечения срока действия.
  - Добавлен бонус "блокировки", если пользователь делал донацию в течение последних двух месяцев.
  - Для поддержки этого модель `User` теперь включает поля `PrioritySearchCount` и `LastDonation`.
  - Модель `Bonus` обновлена и теперь включает поле `Subcategory`.
  - `ImportBonusesHandler` и `GetRecipientDetails` обновлены для использования новой службы бонусов и изменений модели.


## [3.14.3] - 2026-04-20

### Добавлено
- **Добавлено `ignoreSoftDelete` в `GetByApplicationID`, `GetByID` и `GetByUserID`:**
  - В методы `GetByApplicationID` в репозитории поиска крови, а также в методы `GetByID` и `GetByUserID` в репозитории питомцев добавлен новый параметр `ignoreSoftDelete`. Это позволяет получать мягко удаленные сущности при необходимости, особенно для исторических данных в завершенных донациях.

## [3.14.2] - 2026-04-20

### Исправлено
- **Удалены анализы на кошачьи заболевания для собак:**
  - Анализы на "лейкемию" и "иммунодефицит" специфичны для кошачьих и не должны требоваться для собак.

## [3.14.1] - 2026-04-20

### Исправлено
- **Исправлено удаление запросов крови и ответов:**
  - Исправлена ошибка, связанная с удалением запросов крови и ответов.

## [3.14.0] - 2026-04-19

### Добавлено
- **Реализован импорт бонусов из Excel:**
  - Добавлена функция импорта бонусов из файла Excel.
  - Добавлены слои `bonus` (домен, приложение, хранилище).
  - Реализован `ImportBonusesHandler` для парсинга файлов Excel и массового создания бонусов.
  - Добавлена HTTP-конечная точка `/v1/admin/bonuses/import` для импорта бонусов.
  - Обновлена спецификация OpenAPI и сгенерированный клиент TypeScript.
  - Поле `user_id` в схеме `bonus` теперь необязательное.
- **Добавлена сущность `Bonus`:**
  - В схему базы данных добавлена новая сущность `Bonus`.
  - Включает поля `user_id`, `partner_name`, `description`, `target`, `recipient`, `category`, `promo_code`, `expires_at`, `platform_name`, `platform_url` и `is_active`.
  - Добавлено новое ребро `bonuses` к сущности `User`, представляющее отношение "один ко многим" между пользователем и его бонусами.

## [3.13.6] - 2026-04-15

### Добавлено
- **Улучшена логика сопоставления и добавлен `ownerID`:**
  - В функцию `MatchDonor` добавлен `ownerID` для предотвращения сопоставления собственных питомцев донора.
  - Модель `BloodRequest` теперь включает поля `OwnerID` и `Regions` для обеспечения новой логики сопоставления.

### Технические детали
- В `internal/services/matching_service.go` (или соответствующем файле) обновлена функция `MatchDonor` для использования `ownerID`.
- В `internal/domain/bloodsearch/model.go` (или соответствующем файле) в модель `BloodRequest` добавлены поля `OwnerID` и `Regions`.



## [3.13.5] - 2026-04-15

### Исправлено
- **Дедупликация пиров при создании запроса крови:**
  - Предотвращена отправка дублирующих уведомлений одному и тому же пользователю, когда несколько его питомцев соответствуют запросу крови.

## [3.13.4] - 2026-04-15

### Изменено
- **Рефакторинг кодов факторов питомца:**
  - Перемещены `VaccinationExpired`, `DewormingExpired` и `EctoparasiteTreatmentExpired` из стоп-факторов в предупреждающие факторы. Это изменение отражает переоценку их серьезности в процессе донации питомца.

## [3.13.3] - 2026-04-15

### Исправлено
- **Корректный расчет общего количества донаций и объема:**
  - Предыдущая реализация некорректно включала отклоненные и отмененные донации в общий объем и общее количество донаций. Этот коммит гарантирует, что только завершенные донации учитываются в этих суммах.


## [3.13.2] - 2026-04-14

### Добавлено
- **Добавлено `IncludeUnknownBloodGroup` в `RecipientForDonor`:**
  - Добавлено поле `IncludeUnknownBloodGroup` в DTO `RecipientForDonor` и заполнено в обработчике `GetPlannedDonations`.

### Технические детали
- В `dto/recipient.go` (или соответствующем файле) добавлено поле `IncludeUnknownBloodGroup` в `RecipientForDonor` DTO.
- В `internal/handlers/donor/query/get_planned_donations_handler.go` (или соответствующем файле) заполнено поле `IncludeUnknownBloodGroup`.



## [3.13.0] - 2026-04-14

### Добавлено
- **Добавлена привязка местоположения к сопоставлению запросов крови:**
  - Логика сопоставления запросов крови теперь учитывает предпочтительные местоположения донора. Если донор указал предпочтительные местоположения, он будет сопоставляться только с запросами крови, которые пересекаются с этими местоположениями.
- **Добавлено сопоставление типов питомцев в логику донора:**
  - Теперь тип питомца донора должен соответствовать типу питомца получателя для действительного сопоставления.

### Исправлено
- **Исправлен поиск питомцев по региону:**
  - Исправлена проблема, из-за которой питомцы некорректно фильтровались по предпочтительным регионам. Предыдущий запрос некорректно обрабатывал пустые или нулевые `preferred_location_ids` и использовал неэффективный оператор `&&` для проверки вхождения в массив. Обновленный запрос теперь корректно фильтрует питомцев, чьи владельцы имеют предпочтения донора, которые либо содержат один из указанных регионов, либо имеют пустой/нулевой массив `preferred_location_ids`, эффективно сопоставляя все регионы в последнем случае.
- **Исправлена логика сопоставления групп крови:**
  - Корректно сопоставляются неизвестные группы крови, когда `IncludeUnknownBloodGroup` истинно. Ранее любая группа крови сопоставлялась, если этот флаг был установлен.

### Технические детали
- В `internal/services/matching_service.go` (или соответствующем файле) обновлена логика сопоставления для учета предпочтений местоположения донора.
- В `internal/services/matching_service.go` (или соответствующем файле) обновлена логика сопоставления для учета типов питомцев.
- В `internal/repositories/pet/ent_pet_repository.go` (или соответствующем файле) обновлен запрос для корректной фильтрации питомцев по предпочтительным регионам.
- В `internal/domain/bloodsearch/model.go` (или соответствующем файле) обновлена логика сопоставления групп крови.


## [3.12.1] - 2026-04-14

### Добавлено
- **Уведомление доноров о новых запросах крови:**
  - Добавлен новый обработчик событий для `BloodRequestCreated` для уведомления доступных доноров о новых запросах крови через бота Max.
- **Реализовано событие `DonorCompleted`:**
  - Введено новое событие `DonorCompleted` для уведомления получателей, когда донор сообщает о завершенной донации.
  - `CompleteDonationHandler` обновлен для публикации этого события после того, как донация помечена как завершенная. Событие включает детали о питомцах донора и получателя, объем донации и Max ID получателя для уведомления.
  - Добавлен новый обработчик событий `handleDonorCompleted` в бота Max для обработки этого события. Он отправляет сообщение получателю через Telegram, предлагая им подтвердить донацию на портале.

### Изменено
- **Обновлена версия бота Max до 0.5.0.**

### Технические детали
- В `internal/handlers/bloodsearch/cmd/create_request_handler.go` (или соответствующем файле) добавлена публикация события `BloodRequestCreated`.
- В `internal/domain/events.go` (или соответствующем файле) добавлено событие `DonorCompleted`.
- В `internal/handlers/donor/cmd/complete_donation_handler.go` (или соответствующем файле) добавлена публикация события `DonorCompleted`.
- В `max_bot/handlers/handle_donor_completed.go` (или соответствующем файле) добавлен обработчик `handleDonorCompleted`.

### Исправлено
- **Добавлено логирование и исправлена фильтрация регионов питомцев:**
  - Добавлены вызовы `slog.Info` в `CreateRequestHandler` для логирования количества питомцев, найденных по группе крови и региону, а также количества отфильтрованных доступных доноров.
  - Исправлен `GetPetsByBloodGroupAndRegion` для корректной фильтрации питомцев по предпочтительным идентификаторам местоположения, допуская пустые предпочтительные идентификаторы местоположения.

## [3.12.0] - 2026-04-16

### Добавлено
- **Добавлена конечная точка `GetCompletedDonations` для доноров:**
  - Реализована новая конечная точка `/v1/donor/completed-donations/{user_id}`, которая позволяет донорам получать список своих завершенных донаций.
  - Добавлен новый обработчик `CompletedDonationsHandler` и соответствующий DTO `ListCompletedDonationsOutput` для поддержки этой функциональности.
  - В модель `DonorResponse` добавлен метод `IsClosedForDonation()`.

### Технические детали
- В `internal/handlers/donor/query/get_completed_donations_handler.go` (или соответствующем файле) реализован `CompletedDonationsHandler`.
- В `dto/donor.go` (или соответствующем файле) добавлен `ListCompletedDonationsOutput` DTO.
- В `internal/domain/donor/model.go` (или соответствующем файле) добавлен метод `IsClosedForDonation()`.



## [3.11.1] - 2026-04-15

### Изменено
- **Рефакторинг для использования `DonorResponse.IsActiveForDonation()`:**
  - Вся логика, определяющая, является ли ответ донора активным для донации, была перенесена в новый метод `IsActiveForDonation()` в модели `DonorResponse`.
  - Это изменение улучшает инкапсуляцию и упрощает управление статусами доноров.

### Технические детали
- В `internal/domain/donor/model.go` (или соответствующем файле) добавлен метод `IsActiveForDonation()`.
- Обновлены все соответствующие обработчики и сервисы для использования нового метода `IsActiveForDonation()`.

## [3.11.0] - 2026-04-14

### Добавлено
- **Введение `PetService` для расчета статуса питомца:**
  - Реализован новый `PetService`, который инкапсулирует логику расчета статуса питомца, включая проверку наличия активных запросов крови и заявок доноров.
  - `CalculateStatus` теперь использует `PetService` для определения комплексного статуса питомца.

### Технические детали
- В `internal/domain/pet/service.go` (или соответствующем файле) реализован `PetService`.
- В `internal/domain/pet/model.go` (или соответствующем файле) `CalculateStatus` обновлен для использования `PetService`.



## [3.10.4] - 2026-04-13

### Исправлено
- **Заглавные буквы для `UNKNOWN` и установка значения `bloodGroup`:**
  - Значение `UNKNOWN` теперь всегда пишется заглавными буквами.
  - Значение `bloodGroup` теперь корректно устанавливается.

### Изменено
- **Рефакторинг `BloodGroup`:**
  - Компоненты `BloodGroup` теперь жестко заданы.
  - Поле `BloodGroupName` теперь является обязательным.
  - Логика групп крови перемещена в `domain/common`.
  - Группы крови теперь жестко заданы по типу питомца, сущность удалена.


## [3.10.3] - 2026-04-13

### Исправлено
- **Безусловная инициализация структуры `PetHealth`:**
  - Структура `PetHealth` теперь инициализируется безусловно, что предотвращает потенциальные ошибки при доступе к ее полям.
- **Добавлено значение `none` в перечисление `ReproductiveStatus`:**
  - В перечисление `ReproductiveStatus` добавлено значение `none` для более точного представления статуса.
- **Добавлена неизвестная группа крови в перечисления DTO и сиды:**
  - Добавлена возможность указывать неизвестную группу крови в перечислениях DTO и данных для инициализации базы данных.

## [3.10.2] - 2026-04-13

### Изменено
- **Рефакторинг `CalculateStatus` в вспомогательные методы:**
  - Метод `CalculateStatus` был рефакторингован и разделен на более мелкие, вспомогательные методы для улучшения читаемости и поддерживаемости кода.

### Технические детали
- В `internal/domain/pet/model.go` (или соответствующем файле) `CalculateStatus` был разделен на вспомогательные методы.


## [3.10.1] - 2026-04-13

### Добавлено
- **Установка получателя как "перелитого" при подтверждении донации:**
  - Добавлена логика для установки статуса получателя как "перелитого" (transfused) при подтверждении донации.

### Технические детали
- В `internal/handlers/donor/cmd/confirm_donation_handler.go` (или соответствующем файле) добавлена логика установки статуса получателя как "перелитого".



## [3.10.1] - 2026-04-10

### Добавлено
- **Отслеживание последней донации и предупреждение о группе крови:**
  - Добавлено поле `LastDonation` в модель `Pet` для отслеживания даты последней донации.
  - Реализована логика предупреждения, если группа крови питомца не соответствует группе крови, указанной в запросе на донацию.

### Технические детали
- В `internal/domain/pet/model.go` (или соответствующем файле) добавлено поле `LastDonation`.
- В `internal/domain/donor/model.go` (или соответствующем файле) реализована логика предупреждения о группе крови.


## [3.10.0] - 2026-04-10

### Изменено
- **Пакетная выборка заявок доноров и запросов крови по ID питомцев:**
  - Репозитории заявок доноров и запросов крови теперь используют пакетную выборку по ID питомцев для оптимизации производительности.
  - Вспомогательная функция `findActiveApplication` была реализована для выбора наиболее актуальной активной заявки.

### Технические детали
- В `internal/repositories/donor/ent_donor_response_repository.go` и `internal/repositories/bloodsearch/ent_blood_request_repository.go` (или соответствующих файлах) реализованы методы `GetByPetIDs`.
- В `internal/domain/donor/model.go` (или соответствующем файле) добавлена вспомогательная функция `findActiveApplication`.

## [3.9.14] - 2026-04-10

### Исправлено
- **Всегда создавать новые ответы доноров:**
  - Удалена проверка существующих ответов и логика обновления в обработчике `apply`.
  - Оптимизирован `pet GetByUser` с использованием пакетных вызовов репозитория `GetByPetIDs` и вспомогательной функции `findActiveApplication` для выбора наиболее актуального активного ответа.
  - Добавлены реализации `GetByPetIDs` в репозитории доноров и запросов крови.
  - Отключен обход промежуточного ПО аутентификации разработчика.
- **Корректный статус питомца при отклонении или отмене заявок доноров:**
  - Исправлен статус питомца, когда заявки доноров отклоняются или отменяются.



## [3.9.11] - 2026-04-09

### Исправлено
- **Исправление логики `RecalculateStatus` для запросов крови:**
  - Улучшена логика `BloodRequest.RecalculateStatus` для более точного обновления статусов запросов крови.
  - Переименован приватный метод `activate()` в экспортируемый `Activate()`.
  - Добавлено условие `else` для активации, когда зарезервированный объем меньше необходимого.
- **Разрешено отклонение принятых заявок доноров:**
  - Теперь система позволяет отклонять заявки доноров, которые ранее были приняты.
- **Использование `txCtx` для `bloodRepo.GetByApplicationID`:**
  - В `bloodRepo.GetByApplicationID` теперь используется `txCtx` для обеспечения консистентности транзакций.
- **Ужесточение причины отклонения донора до "другое":**
  - Причина отклонения донора теперь жестко установлена на "другое" для стандартизации.
- **Отклонение ожидающих/принятых/неподтвержденных заявок при закрытии запроса крови:**
  - При закрытии запроса крови теперь автоматически отклоняются все ожидающие, принятые и неподтвержденные заявки доноров.

### Добавлено
- **Методы перехода состояний для `DonorResponse`:**
  - Добавлены методы `Accept`, `Reject` (с указанием причины), `Complete`, `Confirm` в модель `DonorResponse` для управления жизненным циклом ответа донора.
  - Добавлено поле `rejected_reason` в модель, схему Ent и миграцию БД.
  - Обновлены обработчики и репозитории для использования этих доменных методов.
  - Исправлена опечатка `RejctedReason` на `RejectedReason`.

### Изменено
- **Исключение отмененных донором запланированных донаций:**
  - Запланированные донации, отмененные донором, теперь исключаются из соответствующих списков и расчетов.


## [3.9.9] - 2026-04-10

### Добавлено
- **Обработчики для завершения и отмены донаций:**
  - Реализованы новые обработчики `CompleteDonationHandler` и `CancelDonationHandler`.
  - Добавлена обработка событий `DonationCompleted` и `DonationCancelled`.
  - Обновлены соответствующие DTO и модели для поддержки новых статусов донаций.

### Технические детали
- В `internal/handlers/donor/cmd/complete_donation_handler.go` (или соответствующем файле) реализован `CompleteDonationHandler`.
- В `internal/handlers/donor/cmd/cancel_donation_handler.go` (или соответствующем файле) реализован `CancelDonationHandler`.
- В `internal/domain/events.go` (или соответствующем файле) добавлены события `DonationCompleted` и `DonationCancelled`.
- Обновлены DTO и модели в соответствующих пакетах (`dto/`, `internal/domain/donor/model.go`).


## [3.9.8] - 2026-04-09

### Изменено
- **Переименованы поля `Name` в `UserName`, добавлены `PetName`, `ProviderTelegram`:**
  - Обновлены структуры событий для использования `UserName` и `PetName`.
  - Добавлено поле `ProviderTelegram`.
  - Удалено неиспользуемое поле `Initiator`.
  - Обновлен код бэкенда и бота для заполнения и использования новых полей.

### Технические детали
- В `internal/domain/events.go` (или соответствующем файле) обновлены структуры событий.
- Обновлен код бэкенда и бота для адаптации к изменениям в структурах событий.


## [3.9.7] - 2026-04-08

### Исправлено
- **Оптимизация выделения срезов и исправление обработки ошибок в запланированных донациях:**
  - Предварительное выделение срезов для повышения производительности.
  - Пропуск записей при ненахождении запросов крови во избежание потенциальных ошибок.
- **Добавлены данные о питомцах донора в ответ на запланированные донации:**
  - Включены данные о питомцах донора в ответ на запланированные донации.
- **Переименованы `BloodGroupNames` в `SearchingBloodNames` в DTO:**
  - Переименовано поле `BloodGroupNames` в `SearchingBloodNames` в DTO для большей ясности.
- **Добавлены `BloodGroup` и `Regions` в DTO карты донаций донора:**
  - Добавлены поля `BloodGroup` и `Regions` в DTO карты донаций донора для предоставления более полной информации.

### Добавлено
- **Публикация подробных событий для подтверждения донации:**
  - Добавлена ​​публикация подробных событий для подтверждения донации.
- **Возврат обогащенного списка питомцев с количеством запланированных донаций:**
  - Обработчик запроса теперь возвращает структурированный результат, содержащий список питомцев, общее количество и количество запланированных донаций.
  - DTO и HTTP-обработчик обновлены для отражения этого нового типа возвращаемого значения.

### Изменено
- **Обновление обработчиков событий донора и получателя:**
  - Обработчики событий донора и получателя были обменены местами.
  - Событие `donation_completed` переименовано в `donation_confirmed`.
  - Связанные файлы обновлены и версия увеличена до 0.4.1.
- **Разрешение повторного подачи заявок донорами и улучшение деталей событий:**
  - Обработчик запроса на подачу заявки обновлен для проверки существующих заявок доноров и их обновления в случае обнаружения, обертывая операции в транзакцию.
  - Детали питомца получателя добавлены в событие `RecipientApply`.


## [3.9.4] - 2026-04-08

### Исправлено
- **Оптимизация выделения срезов и исправление обработки ошибок в запланированных донациях:**
  - Предварительное выделение срезов для повышения производительности.
  - Пропуск записей при ненахождении запросов крови во избежание потенциальных ошибок.
- **Добавлены данные о питомцах донора в ответ на запланированные донации:**
  - Включены данные о питомцах донора в ответ на запланированные донации.
- **Переименованы `BloodGroupNames` в `SearchingBloodNames` в DTO:**
  - Переименовано поле `BloodGroupNames` в `SearchingBloodNames` в DTO для большей ясности.
- **Добавлены `BloodGroup` и `Regions` в DTO карты донаций донора:**
  - Добавлены поля `BloodGroup` и `Regions` в DTO карты донаций донора для предоставления более полной информации.



## [3.9.3] - 2026-04-07

### Исправлено

- **Включение неподтвержденных ответов доноров в запросы крови:**
    - Исправлена ошибка, из-за которой неподтвержденные ответы доноров не включались в результаты запросов крови.

### Добавлено

- **Добавлена конечная точка для получения запланированных донаций:**
    - Реализована новая конечная точка для получения информации о запланированных донациях.

### Изменено

- **Добавлены DTO карты донаций и статус `reserved_full`:**
    - Добавлена структура `DonationCardDTO` для представления информации о карте донаций.
    - Добавлен новый статус `reserved_full` для запросов крови.
    - Переименован файл `donation_card_dto.go` в `donation_card_recipient_dto.go`.
    - Обновлены перечисления статусов в `blood_request_dto.go` и `recipient_dto.go`.


## [3.9.2] - 2026-04-07

### Добавлено
- **Добавлено логирование SQL-запросов с использованием slog и Trace ID:**
  - Реализовано логирование SQL-запросов с использованием пакета `slog` для лучшей отладки и трассировки.
  - Каждый SQL-запрос теперь включает `TraceID` для облегчения сопоставления с соответствующими HTTP-запросами.
  - Добавлены новые параметры конфигурации для управления логированием SQL-запросов.

### Исправлено
- **Устранена проблема с потерей данных при обновлении статуса донора:**
  - Исправлена ошибка, которая могла привести к потере данных при обновлении статуса ответа донора.

### Изменено
- **Обновлены зависимости и улучшена обработка ошибок:**
  - Обновлены основные зависимости проекта до последних стабильных версий.
  - Улучшена обработка ошибок в различных модулях для более надежной работы приложения.


## [3.9.1] - 2026-04-06

### Добавлено
- **Добавлен `TraceIDResponseMiddleware` и удалены отладочные логи:**
  - Создан `req_header.go` для `TraceIDResponseMiddleware`.
  - Интегрирован в основную цепочку HTTP middleware.
  - Старые файлы middleware помечены как устаревшие.
  - Удалены вызовы `slog.DebugContext` из обработчиков.
  - Исправлено форматирование в `file_handler.go`.

### Исправлено
- **Использование `First` вместо `Only` в запросе `GetByPetID`:**
  - В запросе `GetByPetID` используется `First` вместо `Only`.
- **Исправлен `GetByPetID` для упорядочивания результатов по дате создания вместо фильтрации по статусу:**
  - Функция `GetByPetID` теперь упорядочивает результаты по дате создания, а не фильтрует по статусу.
- **Включение `StatusReservedFull` в запрос крови:**
  - Функция `GetByPetID` теперь также возвращает запросы крови со статусом `StatusReservedFull`, а не только `StatusActive`.
- **Добавлен фильтр активного статуса в запрос `GetByPetID`:**
  - В запрос `GetByPetID` добавлен фильтр активного статуса.

### Изменено
- **Изменение связи `Pet-BloodSearchRequest` с один-к-одному на один-ко-многим:**
  - Связь `Pet-BloodSearchRequest` изменена с один-к-одному на один-ко-многим.

## [3.9.0] - 2026-04-06

### Исправлено
- **Добавлено значение по умолчанию для поля `is_confirmed` в схеме `DonorResponse`:**
  - В схему `DonorResponse` добавлено значение по умолчанию для поля `is_confirmed`.

### Изменено
- **Удалено расширение `entgql` и `gqlgen`, изменена связь `Pet-BloodSearchRequest` на M2M:**
  - Удалено расширение `entgql` и `gqlgen`.
  - Связь `Pet-BloodSearchRequest` изменена на M2M (многие ко многим).


## [3.8.12] - 2026-04-05

### Добавлено
- **Обработчик закрытия запроса крови:**
  - Добавлен новый `CloseRequestHandler` для закрытия запросов крови и отклонения ожидающих заявок доноров.

### Изменено
- **Переименование файлов команд:**
  - Файлы команд были переименованы в более короткие названия (например, `create_request.go` в `create_req.go`).


## [3.8.11] - 2026-04-04

### Исправлено
- **Исправление некорректного параметра в вызовах метода репозитория крови:**
  - Параметр, передаваемый в `GetByApplicationID`, был изменен с `application.RequestID` на `donorResponseID` для корректного получения запроса крови. Это обеспечивает согласованность и точность при извлечении соответствующих данных.


## [3.8.10] - 2026-04-04

### Изменено
- **Перемещение операций с запросами крови внутрь транзакции в обработчике принятия ответа:**
  - Операции с запросами крови были перемещены внутрь транзакции для обеспечения согласованности данных. Также добавлено `BloodVolumeDonated` в DTO данных получателя.


## [3.8.9] - 2026-04-04

### Исправлено
- **Обновление статуса ответа донора после подтверждения:**
  - Статус ответа донора теперь корректно обновляется после подтверждения.


## [3.8.8] - 2026-04-04

### Изменено
- **Добавление логирования и контекста транзакции в подтверждение донации:**
  - Подтверждение донора и обновление статуса теперь выполняются в рамках одной транзакции. Добавлено отладочное логирование для обновлений статуса.


## [3.8.7] - 2026-04-04

### Изменено
- **Перемещение подтверждения донора за пределы транзакции в `ConfirmDonationHandler`:**
  - Подтверждение донора теперь выполняется до начала транзакции. Также удалены избыточные вызовы `RecalculateStatus` из обработчиков запросов и обновлен расчет статуса для использования сравнения `>=`.

## [3.8.5] - 2026-04-03

### Исправлено
- **Округление сумм донаций до одного знака после запятой:**
  - Суммы донаций теперь округляются до одного знака после запятой для обеспечения единообразия.
  - Реализована обработка международных форматов ввода десятичных дробей для сумм донаций.

### Технические детали
- В `internal/domain/donor/model.go` (или соответствующем файле) обновлена логика округления сумм донаций.
- В `internal/handlers/donor/cmd/confirm_donation_handler.go` (или соответствующем файле) реализована обработка международных форматов ввода десятичных дробей.


## [3.8.4] - 2026-04-03

### Изменено
- **Изменение полей объема крови с `int32` на `float64`:**
  - Поля, связанные с объемом крови, изменены с `int32` на `float64` для обеспечения большей точности и гибкости в расчетах.

### Технические детали
- Обновлены все соответствующие модели, DTO и логика обработки для использования `float64` вместо `int32` для полей объема крови.

### Исправлено
- **Обновление описаний DTO для закрытия запроса на поиск крови:**
  - Описания DTO обновлены для лучшей ясности и точности при закрытии запросов на поиск крови.

## [3.8.3] - 2026-04-03

### Исправлено
- **Обновление маппинга URL-адресов фотографий получателя в репозитории доноров:**
  - Исправлен маппинг URL-адресов фотографий получателя в репозитории доноров для корректного отображения.

### Технические детали
- В `internal/repositories/donor/ent_donor_repository.go` (или соответствующем файле) обновлена логика маппинга для URL-адресов фотографий получателя.

## [3.8.2] - 2026-04-03

### Исправлено
- **Исправление расчета зарезервированного объема крови и добавление поля донорского объема:**
  - Расчет зарезервированного объема крови теперь корректно накапливает только суммы заявок доноров.
  - Поле `donated_blood_volume` добавлено в DTO `BloodRequestDetail` и маппится из модели.

### Технические детали
- В `internal/domain/bloodsearch/model.go` (или соответствующем файле) обновлена логика расчета зарезервированного объема крови.
- В `dto/blood_search.go` (или соответствующем файле) добавлено поле `donated_blood_volume` в `BloodRequestDetail` DTO.
- В `internal/mappers/blood_request_mapper.go` (или соответствующем файле) обновлена логика маппинга для включения `donated_blood_volume`.

## [3.8.1] - 2026-04-03

### Исправлено
- **Добавлена проверка на `nil` перед вызовом методов `bloodReq`:**
  - Предотвращено потенциальное разыменование нулевого указателя, когда `bloodReq` равен `nil`.

### Технические детали
- В `internal/handlers/bloodsearch/query/get_by_id_handler.go` (или соответствующем файле) добавлена проверка на `nil` для `bloodReq`.

## [3.8.0] - 2026-04-03

### Исправлено
- **Добавлен репозиторий питомцев в обработчик `GetByID` поиска крови:**
  - Репозиторий питомцев добавлен в обработчик `GetByID` поиска крови для получения информации о питомце.

### Технические детали
- В `internal/handlers/bloodsearch/query/get_by_id_handler.go` (или соответствующем файле) добавлен `petRepository`.

## [3.8.0] - 2026-04-03

### Изменено
- **Рефакторинг моделей поиска крови и введение `MatchingService`:**
  - Модели, связанные с поиском крови, объединены в единый пакет.
  - Введен новый `MatchingService` для логики сопоставления доноров.
  - Устаревшие модели получателей удалены.
  - Обновлены типы возвращаемых значений обработчиков и интерфейсы репозиториев для использования новых моделей.

### Технические детали
- В `internal/domain/bloodsearch/models.go` (или соответствующем файле) объединены модели поиска крови.
- В `internal/services/matching_service.go` (или соответствующем файле) введен `MatchingService`.
- Удалены устаревшие модели получателей.
- Обновлены обработчики и репозитории для использования новых моделей.

## [3.7.9] - 2026-04-02

### Изменено
- **Рефакторинг отслеживания объема крови в запросах для использования расчетных значений:**
  - Удалено поле `BloodVolumeReserved` и связанные методы резервирования.
  - Заменено на методы `RecalculateBloodAmount` и `RecalculateStatus`.
  - Все обработчики обновлены для использования нового подхода к расчету.
  - Удален неиспользуемый метод репозитория `UpdateReservedVolume`.
  - Очищена связанная схема базы данных и код Ent.

### Технические детали
- В `internal/domain/bloodsearch/model.go` (или соответствующем файле) удалено `BloodVolumeReserved` и добавлены `RecalculateBloodAmount` и `RecalculateStatus`.
- Обновлены все обработчики для использования новых методов расчета.
- Удален метод `UpdateReservedVolume` из репозитория.
- Очищены связанные файлы схемы базы данных и Ent.

## [3.7.8] - 2026-04-02

### Добавлено
- **Добавлен метод `CalculateDonatedAmount` в модель `BloodRequest`:**
  - Метод рассчитывает общий объем донорской крови на основе подтвержденных и завершенных заявок доноров.

### Технические детали
- В `internal/domain/bloodsearch/model.go` (или соответствующем файле) добавлен метод `CalculateDonatedAmount`.

## [3.7.7] - 2026-04-02

### Добавлено
- **Добавлена конечная точка для закрытия запроса крови по ID:**
  - Добавлено поле `BloodVolumeDonated` в DTO `RecipientShort`.

### Технические детали
- Добавлена новая конечная точка API для закрытия запроса крови.
- В `dto/recipient.go` (или соответствующем файле) добавлено поле `BloodVolumeDonated` в `RecipientShort` DTO.

## [3.7.6] - 2026-04-01

### Изменено
- **Рефакторинг логики подтверждения и отклонения донации:**
  - Перемещение получения запроса крови внутрь транзакции в обработчике подтверждения донации.
  - Условное обновление статуса ответа донора в обработчике отклонения донации.
  - Сделано количество донаций необязательным в подтверждении и обновлена связанная логика.

### Технические детали
- В `internal/handlers/donor/cmd/confirm_donation_handler.go` (или соответствующем файле) логика получения запроса крови перемещена внутрь транзакции.
- В `internal/handlers/donor/cmd/reject_donation_handler.go` (или соответствующем файле) статус ответа донора обновляется условно.
- В `internal/handlers/donor/cmd/confirm_donation_handler.go` (или соответствующем файле) количество донаций сделано необязательным.

## [3.7.5] - 2026-04-01

### Изменено
- **Рефакторинг сводок и описаний DTO для ясности:**
  - Обновлены сводки и описания DTO для улучшения читаемости и ясности.

### Технические детали
- В `dto/...` (или соответствующих файлах) обновлены сводки и описания DTO.

## [3.7.4] - 2026-04-01

### Добавлено
- **Добавлен обработчик контактов пользователя и рефакторинг общих DTO:**
  - Реализован новый `GetContactHandler` для получения контактной информации пользователя.
  - Интерфейс издателя событий перемещен в пакет `domain/ports`.
  - Общие DTO реорганизованы в отдельный пакет.
  - Обновлены имена параметров пути в конечных точках API.

### Технические детали
- В `internal/handlers/user/query/get_contact_handler.go` (или соответствующем файле) реализован `GetContactHandler`.
- В `internal/domain/ports/event_publisher.go` (или соответствующем файле) перемещен интерфейс издателя событий.
- В `dto/common/...` (или соответствующих файлах) реорганизованы общие DTO.
- В `internal/server/server.go` (или соответствующем файле) обновлены имена параметров пути в конечных точках API.


## [3.7.3] - 2026-03-30

### Изменено
- **Рефакторинг получателя: удаление `BloodVolumeRemaining` и добавление флага уведомлений для мелких питомцев:**
  - Поле `BloodVolumeRemaining` теперь рассчитывается динамически вместо постоянного хранения.
  - Добавлено поле `SmallPetsNotifyAllowed` в модель получателя и DTO.
  - Поля объема крови стали необязательными в модели TypeScript.
  - Обновлена схема OpenAPI и логика валидации.

### Технические детали
- В `internal/domain/recipient/model.go` (или соответствующем файле) удалено поле `BloodVolumeRemaining` и добавлено `SmallPetsNotifyAllowed`.
- В `dto/recipient.go` (или соответствующем файле) обновлены DTO для отражения изменений.
- Обновлена схема OpenAPI и логика валидации для учета этих изменений.

## [3.7.2] - 2026-03-30

### Изменено
- **Рефакторинг логики сопоставления доноров получателя:**
  - Логика сопоставления доноров была реорганизована для улучшения читаемости и сопровождается комментариями, объясняющими бизнес-логику, связанную с группами крови и объемом. Расчет объема донации теперь вынесен в отдельную переменную.

### Технические детали
- В `internal/services/donor_matching_service.go` (или соответствующем файле) обновлена логика сопоставления доноров.

### Добавлено
- **Добавлен флаг уведомлений о мелких питомцах в модель получателя:**
  - Модель получателя теперь включает флаг `SmallPetsNotifyAllowed`, который контролирует, следует ли отправлять уведомления для мелких питомцев, способных покрыть как минимум половину оставшегося необходимого объема крови. Логика сопоставления была обновлена для учета этого флага.

### Технические детали
- В `internal/domain/recipient/model.go` (или соответствующем файле) добавлен флаг `SmallPetsNotifyAllowed`.
- В `internal/services/donor_matching_service.go` (или соответствующем файле) обновлена логика сопоставления доноров для учета `SmallPetsNotifyAllowed`.

### Исправлено
- **Исправлено отсутствие оператора return в методе `ReserveVolume`:**
  - В методе `ReserveVolume` был добавлен отсутствующий оператор `return` для корректного завершения выполнения.

### Технические детали
- В `internal/domain/recipient/model.go` (или соответствующем файле) добавлен оператор `return` в метод `ReserveVolume`.

### Удалено
- **Удален закомментированный код из маппера и репозитория питомцев:**
  - Из маппера и репозитория питомцев удален закомментированный код для повышения чистоты кода.

### Технические детали
- В `internal/mappers/pet_mapper.go` и `internal/repositories/pet/ent_pet_repository.go` (или соответствующих файлах) удален закомментированный код.

### Исправлено
- **Удалено отладочное логирование из `ListRequestsHandler`:**
  - Отладочное логирование было удалено из `ListRequestsHandler` для уменьшения шума в логах.

### Технические детали
- В `internal/handlers/donor/query/list_requests.go` (или соответствующем файле) удалены отладочные записи логов.


## [3.7.1] - 2026-03-29

### Исправлено
- **Обновление логики расчета статуса донора питомца:**
  - Рефакторинг `CalculateStatus` для корректной обработки статуса запроса крови и заявок доноров.
  - Теперь проверяет заявки доноров из запроса крови и более точно обновляет условия статуса.

### Технические детали
- В `internal/domain/pet/model.go` (или соответствующем файле) обновлена логика `CalculateStatus` для учета статуса запроса крови и заявок доноров.



## [3.7.0] - 2026-03-29
### Исправлено
- **Исправление паники и обновление логики обработки запросов крови и питомцев:**
  - Добавлены репозитории ответов доноров и запросов крови в `ListRequestsHandler`.
  - Удалены ненужные опции предварительной загрузки питомцев.
  - Упрощен запрос `GetByID` для питомцев.
  - Добавлена информация о владельце в запросы крови и питомцев.
  - Обновлена обработка ошибок для случаев, когда запрос крови не найден.
  - Добавлено отладочное логирование в `ListRequestsHandler`.

### Технические детали
- В `internal/handlers/donor/query/list_requests.go` (или соответствующем файле) добавлены `donorResponseRepo` и `bloodRequestRepo`.
- В `internal/repositories/pet/ent_pet_repository.go` (или соответствующем файле) удалены ненужные опции предварительной загрузки.
- В `internal/repositories/pet/ent_pet_repository.go` (или соответствующем файле) упрощен запрос `GetByID`.
- В `internal/mappers/blood_request_mapper.go` и `internal/mappers/pet_mapper.go` (или соответствующих файлах) добавлена информация о владельце.
- В `internal/handlers/donor/query/list_requests.go` (или соответствующем файле) обновлена обработка ошибок.
- Добавлены отладочные логи в `internal/handlers/donor/query/list_requests.go`.


## [3.6.14] - 2026-03-25

### Добавлено
- **Добавлено поле `AcceptedDonors` в DTO `BloodRequestDetail`:**
  - Поле `AcceptedDonors` добавлено для разделения принятых заявок доноров от ожидающих в ответе.
  - Маппер теперь фильтрует заявки по статусу, помещая принятые в новое поле, а ожидающие — в `Responses`.

### Технические детали
- В `dto/blood_search.go` (или соответствующем файле) добавлено поле `AcceptedDonors` в структуру `BloodRequestDetail`.
- В `internal/mappers/blood_request_mapper.go` (или соответствующем файле) обновлена логика маппинга для фильтрации заявок доноров по статусу и их распределения между `AcceptedDonors` и `Responses`.

## [3.6.13] - 2026-03-25

### Добавлено
- **Публикация события `RecipientApply` в `ApplyForRequestHandler`:**
  - Добавлено новое событие `RecipientApply` для отслеживания заявок получателей.
  - Интегрирована публикация этого события в `ApplyForRequestHandler` через интерфейс `EventPublisher`.
  - Событие `RecipientApply` теперь включает детали донора.

### Технические детали
- В `internal/domain/events.go` (или соответствующем файле) добавлено новое событие `RecipientApply`.
- В `internal/handlers/recipient/cmd/apply_for_request_handler.go` (или соответствующем файле) реализована публикация события `RecipientApply` с деталями донора.
- Обновлен интерфейс `EventPublisher` для поддержки публикации события `RecipientApply`.

## [3.6.8] - 2026-03-24

### Изменено
- **Рефакторинг доменной модели пользователя для включения генерации реферальных URL и маппинга идентификаторов:**
  - Добавлен метод `GenerateRefURL` в модель `Identity` для генерации реферальных URL.
  - Включено поле `Identities` в модель `User` для хранения связанных идентификаторов.
  - Обновлены функции `EntToModel` и `ToResponse` для корректного маппинга поля `Identities`.

### Технические детали
- В `internal/domain/user/model.go` (или соответствующем файле) добавлен метод `GenerateRefURL` в модель `Identity`.
- В `internal/domain/user/model.go` (или соответствующем файле) добавлено поле `Identities` в модель `User`.
- В `internal/mappers/user_mapper.go` (или соответствующем файле) обновлены функции `EntToModel` и `ToResponse` для маппинга `Identities`.

## [3.6.5] - 2026-03-24

### Изменено
- **Добавление маппера пользователя для преобразования моделей Ent в доменные модели:**
  - Реализован новый маппер пользователя для преобразования моделей Ent в доменные модели.

### Технические детали
- Добавлен `internal/mappers/user_mapper.go` (или соответствующий файл) с логикой маппинга пользователя.

## [3.6.5] - 2026-03-24

### Изменено
- **Добавление опции `WithIdentities` для предварительной загрузки пользователя:**
  - Реализована опция `WithIdentities` для предварительной загрузки связанных идентификаторов пользователя.

### Технические детали
- В `internal/repositories/user/ent_user_repository.go` (или соответствующем файле) добавлена опция `WithIdentities`.

## [3.6.5] - 2026-03-24

### Изменено
- **Добавление поля `identities` в структуру `UserDetail` и удаление отладочного лога:**
  - В структуру `UserDetail` добавлено поле `identities` для отображения связанных идентификаторов пользователя.
  - Удален отладочный лог для повышения чистоты кода.

### Технические детали
- В `dto/user.go` (или соответствующем файле) добавлено поле `identities` в структуру `UserDetail`.
- Удалены отладочные записи логов, связанные с этой функциональностью.

## [3.6.5] - 2026-03-23

### Изменено
- **Обновление значений перечисления статуса ответа донора:**
  - Значение `active` заменено на `pending` в целях читаемости.
  - Добавлены новые статусы `completed` и `failed`.

### Технические детали
- В `ent/schema/donor_response.go` (или соответствующем файле) обновлено перечисление статусов ответа донора для включения `pending`, `completed` и `failed`.

## [3.6.0] - 2026-03-23

### Добавлено
- **Добавление репозитория доноров в обработчик создания запроса крови:**
  - Интегрирован репозиторий доноров в обработчик создания запроса крови для получения и обработки доступных доноров на основе группы крови и региона.
  - Обновлен `CreateRequestHandler` для получения и оценки потенциальных доноров.
  - Изменено событие `BloodRequestCreated` для включения информации о доступных донорах.

### Технические детали
- В `internal/handlers/bloodsearch/cmd/create_request_handler.go` (или соответствующем файле) добавлен репозиторий доноров и обновлена логика обработки запроса.
- В `internal/domain/bloodsearch/events.go` (или соответствующем файле) обновлена структура события `BloodRequestCreated`.

## [3.5.20] - 2026-03-23

### Добавлено
- **Добавление издателя событий Redis для создания запроса крови:**
  - Инициализирован клиент Redis в `main.go`.
  - Добавлен издатель в обработчик создания запроса крови.
  - Обновлена структура события `BloodRequestCreated`.
  - Добавлен новый пакет конфигурации Redis.

### Технические детали
- В `main.go` инициализирован клиент Redis.
- В `internal/handlers/bloodsearch/cmd/create_request_handler.go` (или соответствующем файле) добавлен издатель Redis.
- В `internal/domain/bloodsearch/events.go` (или соответствующем файле) обновлена структура события `BloodRequestCreated`.
- Добавлен новый пакет `internal/config/redis`.



## [3.6.0] - 2026-03-22

### Добавлено
- **Обработчик `GetDonorByID` и связанные изменения:**
  - Реализован обработчик `GetDonorByID` в `bloodsearch/query`.
  - Добавлен метод `GetByPetID` в интерфейс репозитория доноров.
  - Добавлено поле `OwnerName` в модель `Pet` и включен владелец в запросы питомцев.
  - Мапперы перемещены в пакет `dtomapper` и обновлены импорты.
  - Обновлены DTO для включения `OwnerName` и `availableBloodAmount`.
  - Сгенерирована новая модель `DonorDetail` в TS клиенте.

### Технические детали
- В `internal/handlers/bloodsearch/query/get_donor_by_id_handler.go` (или соответствующем файле) реализован `GetDonorByID`.
- В `internal/repositories/donor/donor_repository.go` (или соответствующем файле) добавлен метод `GetByPetID`.
- В `internal/domain/pet/model.go` (или соответствующем файле) добавлено поле `OwnerName`.
- В `internal/dtomapper/` (или соответствующем пакете) перемещены и обновлены мапперы.
- В `dto/` (или соответствующих файлах) обновлены DTO для включения `OwnerName` и `availableBloodAmount`.
- В TS клиенте сгенерирована новая модель `DonorDetail`.

## [3.5.19] - 2026-03-22

### Изменено
- **Рефакторинг `DonorApplication` для использования структуры `DonorRestrictions`:**
  - Структура `DonorApplication` обновлена для использования `DonorRestrictions` вместо простых строковых кодов.

### Технические детали
- В `internal/domain/donor/model.go` (или соответствующем файле) обновлена структура `DonorApplication` для использования `DonorRestrictions`.

## [3.5.19] - 2026-03-22

### Изменено
- **Замена `BloodRequestStatus` на строковый тип и добавление конечной точки донора:**
  - Тип `BloodRequestStatus` удален и вместо него используется строковый тип напрямую.
  - Добавлена новая конечная точка для получения запроса крови по ID донора.
  - Добавлена структура DTO `DonorDetail`.
  - Включено `availableBloodAmount` в `PetDetail`.

### Технические детали
- В `internal/domain/bloodsearch/model.go` (или соответствующем файле) удален тип `BloodRequestStatus`.
- В `internal/handlers/bloodsearch/query/get_blood_request_by_donor_id_handler.go` (или соответствующем файле) добавлена новая конечная точка.
- В `dto/donor.go` (или соответствующем файле) добавлена структура `DonorDetail`.
- В `dto/pet.go` (или соответствующем файле) включено `availableBloodAmount` в `PetDetail`.



## [3.5.18] - 2026-03-24

### Исправлено
- **Обогащение предупреждающих факторов заявки донора описаниями:**
  - Преобразование предупреждающих факторов из простых строковых кодов в объекты `RestrictionFactor`.
  - Объекты `RestrictionFactor` теперь содержат как код, так и описание/подробное описание из модели питомца.

### Технические детали
- В `internal/domain/donor/model.go` (или соответствующем файле) обновлена структура `DonorApplication` для использования `RestrictionFactor` вместо строковых кодов для предупреждающих факторов.
- В `internal/domain/pet/model.go` (или соответствующем файле) реализована логика для получения описаний/подробных описаний для предупреждающих факторов.
- В `internal/repositories/donor/ent_donor_response_repository.go` (или соответствующем файле) обновлена логика маппинга для преобразования строковых кодов в объекты `RestrictionFactor` с соответствующими описаниями.



## [3.5.16] - 2026-03-23

### Исправлено
- **Рефакторинг мапперов доменной модели в отдельный пакет и обновление реализаций репозиториев:**
  - Логика маппинга доменной модели из реализаций репозиториев выделена в отдельный пакет `domainmapper`.
  - Добавлены файлы `bloodreq_mapper.go` и `pet_mapper.go` для обработки маппинга между ENT и доменными моделями для запросов крови и питомцев соответственно.
  - Реализации репозиториев обновлены для использования этих мапперов вместо встроенных функций маппинга.

### Технические детали
- В `internal/domainmapper/bloodreq_mapper.go` (или соответствующем файле) реализована логика маппинга для запросов крови.
- В `internal/domainmapper/pet_mapper.go` (или соответствующем файле) реализована логика маппинга для питомцев.
- В `internal/repositories/bloodsearch/ent_blood_request_repository.go` (или соответствующем файле) и `internal/repositories/pet/ent_pet_repository.go` (или соответствующем файле) обновлены реализации для использования новых мапперов.

## [3.5.14] - 2026-03-22

### Исправлено
- **Удален расчет суммы заявки донора и отладочный лог:**
  - Расчет суммы заявки донора удален, так как теперь он обрабатывается непосредственно в репозитории.
  - Отладочный лог удален для уменьшения шума.

### Технические детали
- В `internal/services/donor_response_service.go` (или соответствующем файле) удален расчет суммы заявки донора.
- В `internal/repositories/donor/ent_donor_response_repository.go` (или соответствующем файле) удален отладочный лог.

## [3.5.13] - 2026-03-21

### Исправлено
- **Рефакторинг логики сопоставления запросов крови:**
  - Логика сопоставления доноров перемещена из репозитория в прикладной слой.
  - Метод `AdaptiveList` упрощен за счет удаления логики сопоставления доноров.
  - Метод `AddMatchingDonor` переименован в `MatchDonor` для ясности.

### Технические детали
- В `internal/repositories/donor/ent_donor_response_repository.go` (или соответствующем файле) удалена логика сопоставления доноров из `AdaptiveList`.
- В `internal/services/donor_matching_service.go` (или соответствующем файле) реализована логика сопоставления доноров.
- В `internal/domain/donor/model.go` (или соответствующем файле) метод `AddMatchingDonor` переименован в `MatchDonor`.

## [3.5.12] - 2026-03-20

### Исправлено
- **Оптимизация фильтрации групп крови в запросе `AdaptiveList`:**
  - Ручное создание среза `any` заменено использованием карты для дедупликации групп крови и `pq.Array` для обработки массивов PostgreSQL.
  - Это улучшает производительность и корректность, избегая дублирования записей групп крови в запросе.

### Технические детали
- В `internal/repositories/donor/ent_donor_response_repository.go` (или соответствующем файле) логика фильтрации групп крови обновлена для использования карты для дедупликации и `pq.Array`.

## [3.5.11] - 2026-03-20

### Исправлено
- **Удален неиспользуемый импорт sqljson и упрощен запрос фильтрации групп крови:**
  - Удален неиспользуемый импорт `sqljson` из `ent_blood_request_repository.go`.
  - Упрощена логика запроса фильтрации групп крови в `ent_blood_request_repository.go`.

### Технические детали
- В `internal/repositories/bloodsearch/ent_blood_request_repository.go` (или соответствующем файле) удален импорт `sqljson`.
- В `internal/repositories/bloodsearch/ent_blood_request_repository.go` (или соответствующем файле) упрощена логика запроса фильтрации групп крови.


## [3.5.10] - 2026-03-20

### Исправлено
- **Добавлено поле `amount` в модель `DonorResponse` и обновлены связанные методы:**
  - В модель `DonorResponse` добавлено поле `amount` для отслеживания объема донорской крови.
  - Обновлены конструктор, методы репозитория и схема базы данных для поддержки нового поля.
  - Исправлено название метода `AdptiveList` на `AdaptiveList`.

### Технические детали
- В `ent/schema/donor_response.go` (или соответствующем файле) добавлено поле `field.Float("amount")`.
- В `internal/domain/donor/model.go` (или соответствующем файле) обновлен конструктор `DonorResponse` для включения поля `amount`.
- В `internal/repositories/donor/ent_donor_response_repository.go` (или соответствующем файле) обновлены методы репозитория для сохранения и извлечения поля `amount`.
- В `internal/repositories/donor/ent_donor_response_repository.go` (или соответствующем файле) исправлено название метода `AdptiveList` на `AdaptiveList`.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent.




## [3.5.9] - 2026-03-20

### Исправлено
- **Обработка заявок доноров и обновление модели запроса крови:**
  - Включение заявок доноров в модель запроса крови.
  - Обновление расчета статуса питомца для запросов крови.
  - Маппинг заявок доноров в репозитории запросов крови.
  - Корректировка опций предварительной загрузки для запросов питомцев.
  - Улучшение маппера запросов крови для обработки заявок доноров.

### Технические детали
- В `internal/domain/bloodsearch/model.go` (или соответствующем файле) добавлено поле `DonorApplications`.
- В `internal/services/pet_service.go` (или соответствующем файле) обновлена логика расчета статуса питомца для учета заявок на кровь.
- В `internal/repositories/bloodsearch/ent_blood_request_repository.go` (или соответствующем файле) обновлена логика маппинга для включения `DonorApplications`.
- В `internal/repositories/pet/ent_pet_repository.go` (или соответствующем файле) скорректированы опции предварительной загрузки для запросов питомцев.
- В `internal/mappers/blood_request_mapper.go` (или соответствующем файле) обновлена логика маппинга для обработки `DonorApplications`.



## [3.5.7] - 2026-03-20

### Изменено
- **Обновление конструкторов обработчиков для использования указателя TxManager:**
  - Конструкторы обработчиков в пакетах `bloodsearch` и `donor` были изменены для принятия `TxManager` в качестве указателя вместо значения.
  - Это обеспечивает согласованное использование по всему приложению.

### Технические детали
- В `internal/handlers/bloodsearch/blood_search_handler.go` и `internal/handlers/donor/donor_handler.go` (или соответствующих файлах) конструкторы обработчиков были обновлены для принятия `*transaction.TxManager` вместо `transaction.TxManager`.



## [3.5.6] - 2026-03-20

### Исправлено
- **Замена удаления питомца каскадной операцией удаления:**
  - Новый метод `DeleteWithRelations` теперь обрабатывает удаление всех связанных сущностей (записей о здоровье, процедур, анализов, запросов и ответов на кровь) при удалении питомца.
  - Это заменяет предыдущий подход ручного удаления в обработчике.
  - Пропуск middleware аутентификации в среде разработки для упрощения тестирования.

### Технические детали
- В `internal/services/pet_service.go` (или соответствующем файле) реализован метод `DeleteWithRelations`.
- В `internal/handlers/pet/cmd/delete_handler.go` (или соответствующем файле) обновлена логика удаления для использования `DeleteWithRelations`.
- В `internal/server/server.go` (или соответствующем файле) добавлена логика для пропуска middleware аутентификации в среде разработки.

## [3.5.5] - 2026-03-19

### Исправлено
- **Фильтрация запросов крови для включения только тех, у кого есть подходящие доноры:**
  - Логика фильтрации запросов крови обновлена для показа только тех запросов, у которых есть хотя бы один подходящий донор.

### Технические детали
- В `internal/handlers/donor/query/list_requests.go` (или соответствующем файле) добавлена фильтрация для включения только запросов крови с подходящими донорами.

## [3.5.4] - 2026-03-19

### Добавлено
- **Поле `IncludeUnknownBloodGroup` в модель получателя:**
  - Добавлено новое поле `IncludeUnknownBloodGroup` (булево) в модель `Recipient` для учета неизвестных групп крови при подборе доноров.

### Изменено
- **Расширение модели `Recipient` и логики подбора доноров:**
  - Модель `Recipient` и логика подбора доноров были расширены для включения нового поля `IncludeUnknownBloodGroup`.
  - Логика добавления подходящих доноров обновлена для обработки неизвестных групп крови.
  - Поле `IncludeUnknownBloodGroup` теперь маппится на уровне репозитория при конструировании объекта `Recipient`.

### Технические детали
- В `internal/domain/recipient/model.go` (или соответствующем файле) добавлено поле `IncludeUnknownBloodGroup` (bool).
- В `internal/repositories/donor/ent_donor_repository.go` (или соответствующем файле) обновлена логика маппинга для включения `IncludeUnknownBloodGroup` при создании объекта `Recipient`.
- В `internal/services/donor_matching_service.go` (или соответствующем файле) обновлена логика подбора доноров для учета `IncludeUnknownBloodGroup`.

## [3.5.3] - 2026-03-19

### Изменено
- **Использование переменной окружения для токена Telegram бота в тестах:**
  - Токен Telegram бота в тестовых конфигурациях теперь извлекается из переменной окружения `TELEGRAM_BOT_TOKEN` вместо использования жестко закодированного значения.

### Технические детали
- В `internal/config/config.go` (или соответствующем файле конфигурации тестов) обновлена логика получения токена Telegram бота для использования `os.Getenv("TELEGRAM_BOT_TOKEN")`.

## [3.5.2] - 2026-03-20

### Изменено
- **Рефакторинг обработки ответа донора с поддержкой транзакций:**
  - Проверка существования донора заменена предварительной загрузкой питомца для оптимизации.
  - Расчет количества донаций перемещен в модель `Pet` для централизации бизнес-логики.
  - Добавлена поддержка транзакций для атомарного создания ответа донора.
  - Удалены избыточные обновления временных меток в `BloodRequest`.
  - Добавлен метод `UpdateReservedVolume` в `BloodRequestRepository` для обновления зарезервированного объема крови.

### Технические детали
- В `internal/handlers/donor/cmd/create_donor_response_handler.go` логика проверки существования донора изменена на предварительную загрузку питомца.
- В `internal/domain/pet/model.go` реализована логика расчета количества донаций.
- В `internal/services/donor_response_service.go` (или соответствующем файле) добавлена транзакционная логика для создания ответа донора.
- В `internal/domain/bloodsearch/model.go` (или соответствующем файле) удалены избыточные обновления временных меток.
- В `internal/repositories/bloodsearch/blood_request_repository.go` добавлен метод `UpdateReservedVolume(ctx context.Context, bloodRequestID string, volume float64) error`.

## [3.5.0] - 2026-03-18

### Добавлено
- **Новые статусы питомцев и статус ответа донора в виде перечисления:**
  - Добавлены новые статусы питомцев: 'recovering' и 'planned_donation'.
  - Добавлено поле 'planingDonation' в модель Pet.
  - Добавлена опция предварительной загрузки 'WithDonorApplication'.
  - Статус ответа донора преобразован в перечисление со значениями: active, accepted, rejected, cancelled.

### Изменено
- **Расширение GetPetsByUser дополнительными метаданными:**
  - Ответ теперь включает общее количество питомцев и запланированных донаций для пользователя.

### Технические детали
- В `ent/schema/pet.go` добавлены новые статусы 'recovering' и 'planned_donation' в перечисление статусов питомцев.
- В `internal/domain/pet/model.go` добавлено поле 'planingDonation' в модель Pet.
- В `internal/repositories/pet/ent_pet_repository.go` добавлена опция предварительной загрузки 'WithDonorApplication'.
- В `ent/schema/donor_response.go` статус ответа донора преобразован в перечисление с значениями active, accepted, rejected, cancelled.
- В `internal/handlers/pet/query/get_by_user_handler.go` обновлена логика для включения общего количества питомцев и запланированных донаций в ответ.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent.

## [3.4.11] - 2026-03-19

### Изменено
- **Расширение модели получателя с дополнительной информацией и отслеживанием объема крови:**
  - В модель `Recipient` добавлены поля `BloodVolumeNeeded` и `BloodVolumeReserved` для отслеживания необходимого и зарезервированного объема крови.
  - В модель `Recipient` введена вложенная структура `AdvancedInfo`, содержащая `description` и `photos`.
  - Фотографии питомца теперь перемещены в `AdvancedInfo`, а основные `PhotoURLs` получателя используют фотографии питомца.
  - В `MatchingDonorReadModel` добавлено поле `PetID` для лучшей идентификации.
  - Упрощена логика обработки предпочтений по умолчанию в обработчике получения получателя.

### Технические детали
- В `internal/domain/recipient/model.go` (или соответствующем файле) добавлены поля `BloodVolumeNeeded` (float64) и `BloodVolumeReserved` (float64), а также структура `AdvancedInfo` с полями `description` (string) и `photos` ([]string).
- В `internal/domain/recipient/model.go` (или соответствующем файле) логика `PhotoURLs` обновлена для использования `AdvancedInfo.photos`.
- В `internal/domain/donor/model.go` (или соответствующем файле) в `MatchingDonorReadModel` добавлено поле `PetID` (string).
- В `internal/handlers/donor/query/recipient_detail_handler.go` (или соответствующем файле) упрощена логика инициализации и возврата предпочтений по умолчанию.
- Обновлены соответствующие DTO и мапперы для отражения изменений в модели `Recipient` и `MatchingDonorReadModel`.


## [3.4.10] - 2026-03-18

### Изменено
- **Интеграция файлового хранилища в обработчики доноров и улучшение подбора доноров:**
  - Репозиторий файлового хранилища теперь включается при инициализации обработчиков доноров.
  - Расчет количества донаций перемещен в модель получателя для централизации логики.
  - Удалено отладочное логирование из запросов доноров для повышения производительности и чистоты кода.
  - Сервис хранилища используется для построения URL-адресов фотографий с временными метками.
  - Полный объект питомца теперь передается в `AddMatchingDonor` для более полной информации.
  - Текущее время кэшируется в переменных, чтобы избежать повторных вызовов `time.Now()`.

### Технические детали
- В `internal/handlers/donor/query/query.go` (или соответствующем файле инициализации обработчиков доноров) добавлен `fileStorageRepository` в конструктор.
- В `internal/domain/recipient/model.go` (или соответствующем файле) реализована логика расчета количества донаций.
- В `internal/handlers/donor/...` (и других файлах, где были запросы доноров) удалены все отладочные записи логов.
- В `internal/handlers/donor/...` и `internal/repositories/donor/...` обновлена логика для использования `storageService.BuildPhotoURLs` с временными метками.
- В `internal/domain/donor/model.go` (или соответствующем файле) метод `AddMatchingDonor` теперь принимает полный объект `pet.Pet` вместо отдельных полей.
- В `internal/handlers/donor/...` и `internal/services/donor/...` (или соответствующих файлах) `time.Now()` вызывается один раз и результат сохраняется в переменной для дальнейшего использования.

## [3.4.9] - 2026-03-17

### Изменено
- **Рефакторинг модели `DonorPreference` для упрощения опций компенсации:**
  - Массив `conditions` в модели `DonorPreference` заменен на отдельные поля `compensation_type` (перечисление) и `taxi_compensation` (булево).
  - Все поля в `DefaultDonorPrefs` теперь обязательны.
  - Удалено поле `petType` из модели `MatchingDonor`.
  - Обновлены связанные перечисления и логика валидации.

### Технические детали
- В `ent/schema/donor_preference.go` удалено поле `conditions`, добавлены `field.Enum("compensation_type").Values(...)` и `field.Bool("taxi_compensation")`.
- В `internal/domain/donor/model.go` (или соответствующем файле) обновлена структура `DefaultDonorPrefs` для обеспечения обязательности всех полей.
- В `internal/domain/donor/model.go` (или соответствующем файле) удалено поле `petType` из модели `MatchingDonor`.
- В `dto/donor_preference.go` обновлены DTO для отражения изменений в полях компенсации и удалении `petType`.
- Обновлены соответствующие обработчики и сервисы, которые взаимодействуют с `DonorPreference` и `MatchingDonor`, для использования новых полей и валидации.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent.


## [3.4.8] - 2026-03-16

### Добавлено
- **Middleware аутентификации с контролем доступа на основе ролей (RBAC):**
  - Реализован JWT-аутентификационный middleware с возможностью исключения определенных путей.
  - Добавлены вспомогательные функции для извлечения ID пользователя и его роли из контекста запроса.
  - Включены middleware для проверки требований к ролям (`RequireRole`, `RequireAnyRole`).
  - Обновлена цепочка middleware сервера для включения аутентификационного middleware.
  - Прямой доступ к контексту для получения ID пользователя заменен на использование `middleware.GetUserID` в обработчиках.

### Технические детали
- В `internal/middleware/auth_middleware.go` реализован `AuthMiddleware` для проверки JWT-токенов и `ExcludePaths` для определения исключений.
- В `internal/middleware/auth_middleware.go` добавлены функции `GetUserIDFromContext` и `GetUserRoleFromContext`.
- В `internal/middleware/role_middleware.go` реализованы `RequireRole` и `RequireAnyRole` для проверки ролей пользователя.
- В `internal/server/server.go` обновлена инициализация сервера для включения `AuthMiddleware` в цепочку.
- В `internal/handlers/...` и `internal/services/...` прямой доступ к `ctx.Value("userID")` заменен на `middleware.GetUserIDFromContext(ctx)`.

## [3.4.7] - 2026-03-15

### Добавлено
- **Обработчик деталей получателя для операций донора:** Добавлен новый функционал для получения деталей получателя, включая соответствующих доноров и предпочтения по умолчанию.

### Технические детали
- В `internal/repositories/donor_repository.go` реализован метод `GetRecipient` для получения данных о получателе.
- В `internal/domain/recipient.go` добавлена модель `Recipient` с полями для соответствующих доноров и предпочтений по умолчанию.
- В `internal/handlers/donor/query/recipient_detail_handler.go` создан `RecipientDetailHandler` для обработки запросов на получение деталей получателя.
- В `internal/handlers/donor/donor_handler.go` зарегистрирована новая конечная точка для `RecipientDetailHandler`.
- В `dto/recipient.go` обновлены структуры DTO для ответа с деталями получателя, включая соответствующих доноров и предпочтения.
```

## [3.4.6] - 2026-03-14

### Добавлено
- **Поле `PetType` в модели получателя и DTO:** Добавлено поле `PetType` в модель `Recipient` и соответствующее DTO. Это поле включает валидацию перечисления для значений "dog" и "cat".

### Изменено
- **Расширение модели `Recipient` и DTO:** Модель `Recipient` и DTO были расширены для включения нового поля `PetType`.
- **Обновление маппинга через все слои:** Поле `PetType` теперь маппится через все слои приложения, от базы данных до HTTP-ответа.
- **Валидация перечисления в DTO:** В DTO добавлена валидация перечисления для `PetType` со значениями "dog" и "cat".

### Технические детали
- В `ent/schema/recipient.go` добавлено поле `field.Enum("pet_type").Values("dog", "cat")`.
- В `dto/recipient.go` добавлено поле `PetType string `json:"petType" validate:"oneof=dog cat"``.
- В `internal/mappers/recipient_mapper.go` обновлена логика маппинга для включения `PetType`.
- В `internal/handlers/recipient/query/get_by_id_handler.go` и других соответствующих обработчиках обновлена логика для включения `PetType` в ответы.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent.


## [3.4.4] - 2026-03-13

### Добавлено
- **Поля статуса поиска крови в модели питомца:** Добавлены новые поля `SearchingBlood` (булево) и `HaveBloodReqApplication` (булево) в модель `Pet` для отслеживания статуса поиска крови.
- **Новое значение `RECIPIENT_LIST` в перечислении онбординга:** Добавлено новое значение `RECIPIENT_LIST` в перечисление `OnBoarding` для расширения возможностей онбординга.

### Изменено
- **Обновлена версия API до 3.4.4:** Версия API обновлена до 3.4.4.
- **Обновлена схема Ent и сгенерированные файлы:** Схема Ent была обновлена для включения новых полей `searching_blood` и `have_blood_req_application` в сущность `Pet`, а также для добавления нового значения в перечисление `OnBoarding`. Соответствующие сгенерированные файлы Ent были обновлены.
- **Расширены DTO и обработчики для поддержки новых полей и значения:** DTO `Pet` и связанные обработчики были обновлены для корректной обработки и использования новых полей и значения перечисления.

### Технические детали
- В `ent/schema/pet.go` добавлены поля `field.Bool("searching_blood").Default(false)` и `field.Bool("have_blood_req_application").Default(false)`.
- В `ent/schema/bloodsearchrequest.go` (или соответствующем файле, где определено перечисление `OnBoarding`) добавлено значение `RECIPIENT_LIST` в перечисление.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent.
- В `dto/pet.go` добавлены поля `SearchingBlood bool `json:"searchingBlood"` и `HaveBloodReqApplication bool `json:"haveBloodReqApplication"`.
- В `dto/blood_search.go` обновлено перечисление `OnBoarding` для включения `RECIPIENT_LIST`.
- В `internal/handlers/pet/query/get_by_id_handler.go` и `internal/handlers/pet/query/get_by_user_handler.go` обновлена логика для включения новых полей в ответы.
- В конфигурации Huma API обновлена версия до 3.4.4.


## [3.4.3] - 2026-03-12

### Удалено
- **Удаление интеграции с Telegram и факторов питомца:**
  - Удалено поле `TelegramID` из модели пользователя и связанный обработчик запросов.
  - Удалены поля `stop_factors` и `warn_factors` из модели питомца.
  - Очищены связанные определения GraphQL и схемы API.

### Технические детали
- В `ent/schema/user.go` удалено поле `TelegramID`.
- В `internal/handlers/user/query/get_by_telegram_id_handler.go` удален обработчик `GetByTelegramIDHandler`.
- В `ent/schema/pet.go` удалены поля `stop_factors` и `warn_factors`.
- Обновлены соответствующие GraphQL схемы и резолверы для удаления `TelegramID`, `stop_factors` и `warn_factors`.
- Обновлены DTO и валидация для отражения этих изменений.


## [3.4.2] - 2026-03-12

### Изменено
- **Инициализация обработчика запросов доноров и обновление логики репозитория питомцев:**
  - Обработчик запросов доноров теперь инициализируется с использованием обработчика запросов списка получателей крови.
  - Репозиторий питомцев обновлен для предварительной загрузки данных о заявках на кровь.
  - Обработчик питомцев теперь включает данные о заявках на кровь в свои ответы.

### Технические детали
- В `internal/handlers/donor/query/query.go` добавлена инициализация `listRecipientsQueryHandler`.
- В `internal/repositories/pet/ent_pet_repository.go` обновлена логика для предварительной загрузки `BloodRequests` при получении питомцев.
- В `internal/handlers/pet/query/get_by_id_handler.go` и `internal/handlers/pet/query/get_by_user_handler.go` обновлена логика для включения данных о заявках на кровь в ответы.


## [3.4.0] - 2026-03-12

### Изменено
- **Рефакторинг обработки заявок на кровь и удаление обработчика списка:**
  - Удален неиспользуемый `bloodListHandler` и связанный с ним код.
  - Файл `list_requests.go` переименован и перемещен в пакет `donor`.
  - `ListRequestsHandler` обновлен для использования доменной модели донора и репозитория питомцев.
  - Упрощена логика удаления заявок на кровь в обработчике удаления питомцев.
  - Удален неиспользуемый `bloodrequest_service.go`.
  - Интерфейс `BloodRequestRepository` обновлен для использования доменной модели донора.
  - Добавлены новые определения доменной модели донора.
  - `EntBloodRequestRepository` обновлен для использования новой доменной модели донора.
  - `listHandler` удален из инициализации `BloodRequestHandler`.

### Технические детали
- Удален `bloodListHandler` из `internal/handlers/bloodsearch/query/list_requests_handler.go` и `internal/handlers/bloodsearch/query/query.go`.
- `internal/handlers/bloodsearch/query/list_requests.go` переименован и перемещен в `internal/handlers/donor/query/list_requests.go`.
- В `internal/handlers/donor/query/list_requests_handler.go` обновлена логика для использования `domain.Donor` и `pet.Repository`.
- В `internal/handlers/pet/cmd/delete_handler.go` упрощена логика удаления связанных заявок на кровь.
- Удален `internal/domain/bloodsearch/bloodrequest_service.go`.
- В `internal/repositories/bloodsearch/blood_request_repository.go` обновлен интерфейс `BloodRequestRepository` для работы с `domain.Donor`.
- В `internal/domain/donor/model.go` добавлены новые определения доменной модели донора.
- В `internal/repositories/bloodsearch/ent_blood_request_repository.go` обновлена реализация для использования `domain.Donor`.
- В `internal/handlers/bloodsearch/blood_search_handler.go` удален `listHandler` из инициализации.

## [3.3.13] - 2026-03-12

### Изменено
- **Пересчет факторов питомца в query-обработчиках:**
  - Метод `RecalculateFactors` теперь вызывается для каждого питомца в обработчиках `GetByID` и `GetByUser` для обеспечения актуальности факторов.

### Технические детали
- В `internal/handlers/pet/query/get_by_id_handler.go` и `internal/handlers/pet/query/get_by_user_handler.go` добавлена логика вызова `pet.RecalculateFactors()` после получения питомца(ов) из репозитория.


## [3.3.12] - 2026-03-11

### Изменено
- **Рефакторинг валидации аутентификации и упрощение схемы партнера:**
  - **Замена `MiniAppValidator` на `SimpleValidator` и удаление `ApiKeysValidator`:** Валидаторы для Telegram и Max теперь объединены в единый `SimpleValidator`. `ApiKeysValidator` был удален.
  - **Удаление поля `provider_name` из схемы `Partner`:** Поле `provider_name` и связанный с ним код были удалены из схемы `Partner`, что упрощает управление партнерами.
  - **Обновление обработчиков аутентификации:** Обработчики аутентификации были обновлены для использования нового унифицированного подхода к валидации.
  - **Очистка неиспользуемого кода:** Удален неиспользуемый код и упрощено управление идентификаторами для повышения эффективности и читаемости.

### Технические детали
- В `internal/validators/` `telegram_validator.go` и `max_validator.go` были объединены в `simple_validator.go`. `api_key_validator.go` был удален.
- В `ent/schema/partner.go` удалено поле `provider_name`.
- В `internal/handlers/auth_handler.go` и `internal/services/auth_service.go` обновлена логика для использования `SimpleValidator`.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent после изменения схемы `Partner`.


## [3.3.6] - 2026-03-09

### Изменено
- **Рефакторинг обработчиков аутентификации и генерации токенов:**
  - Заменены жестко закодированные значения на переменные окружения для JWT и токенов ботов.
  - Введены отдельные валидаторы для провайдеров Telegram и Max.
  - Добавлен `ApiKeysValidator` для валидации API-ключей партнёров.
  - Реализована генерация JWT-токенов с проверкой срока действия и издателя.
  - Обновлен `MiniAppSignInHandler` для поддержки нескольких провайдеров.
  - Улучшена валидация `WebAppInitData` с проверкой HMAC-подписи.

### Технические детали
- В `internal/handlers/auth_handler.go` и `internal/services/auth_service.go` обновлена логика аутентификации для использования переменных окружения для JWT-секретов и токенов ботов.
- В `internal/validators/` добавлены `telegram_validator.go`, `max_validator.go` и `api_key_validator.go` для специализированной валидации.
- В `internal/services/auth_service.go` реализована генерация JWT с использованием библиотеки `jwt-go` (или аналогичной) с установкой срока действия и проверкой издателя.
- `MiniAppSignInHandler` в `internal/handlers/auth_handler.go` был модифицирован для динамического выбора валидатора на основе `provider_name`.
- В `internal/validators/` обновлена логика валидации `WebAppInitData` для включения проверки HMAC-подписи.

```

## [3.3.5] - 2026-03-10

### Изменено
- **Обновление конфигурационных значений аутентификации:** Изменён секрет JWT на "some-token". Изменены данные инициализации валидатора с "inbotdata" на "some-init-data". Сделано поле partner ID опциональным в upsert для UserIdentity.

### Технические детали
- В конфигурационных файлах изменён секрет JWT на "some-token".
- В `internal/validators/secret_validator.go` обновлены данные инициализации с "inbotdata" на "some-init-data".
- В `ent/schema/user_identity.go` поле partner ID сделано опциональным в логике upsert.

## [3.3.4] - 2026-03-09

### Добавлено
- **Сущность Partner:** Введена новая сущность `Partner` для управления внешними партнёрами (клиниками, сервисами) с полями для API-ключей, ролей и статуса. Добавлена связь с `UserIdentity`.
- **Роль клиники:** Добавлена новая роль `RoleClinic` для пользователей и партнёров, с динамическим присвоением на основе префикса provider ID.
- **Поддержка partner ID в аутентификации:** Добавлено поле partner ID в модель аутентификации, передаваемое через весь поток аутентификации.

### Изменено
- **Рефакторинг валидации аутентификации:** Замена hardcoded API-ключей на lookup через PartnerRepository. Изменение `provider_user_id` с int64 на string. Обновление методов валидации для возврата информации о роли.
- **Обновление имён провайдеров:** Изменены имена провайдеров с "telegram_miniapp" и "max_miniapp" на "telegram_bot" и "max_bot".
- **Рефакторинг upsert для UserIdentity:** Изменена колонка конфликта с (user_id, provider) на (provider_user_id, provider).
- **Упрощение логики аутентификации:** Удалены неиспользуемые функции и код, улучшена обработка ролей и валидации.

### Технические детали
- В `ent/schema/partner.go` добавлена новая схема с полями для API-ключей, ролей и статуса.
- В `internal/domain/partner.go` и `internal/repositories/partner_repository.go` реализована доменная модель и репозиторий для партнёров.
- В `internal/services/auth_service.go` и `internal/handlers/auth_handler.go` добавлена поддержка partner ID и рефакторинг валидации.
- В `ent/schema/user_identity.go` обновлён upsert для использования (provider_user_id, provider).
- В `internal/validators/secret_validator.go` изменена логика валидации для работы с партнёрами и строковыми ID.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent.


## [3.3.3] - 2026-03-08

### Изменено
- **Рефакторинг аутентификации пользователя для обработки регистрации и метаданных:** Замена простого создания пользователя на унифицированный обработчик аутентификации. Добавлена поддержка транзакций для создания пользователя и обновления метаданных. Обновлена модель идентичности для включения данных инициализации приложения и токена. Удален устаревший простой обработчик создания и связанный код. Добавлен уникальный индекс для комбинации провайдера и идентичности пользователя.

### Технические детали
- В `internal/handlers/user_handler.go` заменен простой обработчик создания на унифицированный `AuthHandler` для обработки регистрации и метаданных.
- Добавлена транзакционная логика в `internal/services/user_service.go` для атомарности операций создания пользователя и обновления метаданных.
- В `ent/schema/user_identity.go` добавлены поля для данных инициализации приложения и токена.
- Удален `CreateSimpleHandler` из `internal/handlers/user_handler.go` и связанный код.
- В схеме `UserIdentity` добавлен уникальный индекс для комбинации `provider_id` и `provider_name`.

## [3.3.2] - 2026-03-07

### Удалено
- **Удаление предпочтений донора по умолчанию из регистрации пользователя:** Убрана логика автоматического создания предпочтений донора по умолчанию при регистрации нового пользователя, чтобы дать пользователям возможность самостоятельно настраивать свои предпочтения.

### Технические детали
- В `internal/services/user_service.go` удален код инициализации `DonorPreference` по умолчанию в методе создания пользователя.
```

## [3.3.1] - 2026-03-06

### Изменено
- **Обновление логики upsert для предпочтений донора:** Исправлена логика upsert в сущности `DonorPreference`, где колонка конфликта была некорректно установлена на "user_donor_preference". Теперь используется правильная колонка "user_id" для обеспечения корректного обновления записей.

### Технические детали
- В коде upsert для `DonorPreference` удален дублированный вызов `OnConflict(sql.ConflictColumns("user_donor_preference"))` и оставлен только `OnConflict(sql.ConflictColumns("user_id"))`, что предотвращает ошибки при конфликтах и обеспечивает атомарность операций.
```


## [3.3.0] - 2026-03-05

### Добавлено
- **Новая сущность UserIdentity:** Введена сущность `UserIdentity` для поддержки аутентификации через внешние провайдеры, включая поля для провайдера, ID пользователя провайдера и метаданные. Установлена связь O2M с сущностью `User`.
- **Эндпоинты аутентификации пользователей:** Добавлены новые эндпоинты для аутентификации пользователей, включая модели `AuthUserBody` и `AuthUserResult`. Введены поля `ProviderID` и `ProviderName` в модель `User`, заменяющие `TelegramID`, для поддержки различных мессенджеров.
- **Отслеживание UTM-истории:** Добавлена сущность `UtmHistory` для отслеживания UTM-параметров (source, medium, campaign, content, term) при регистрации пользователей. В модель `User` добавлено поле `OriginSource` для хранения источника привлечения.
- **Поддержка транзакций в операциях с пользователями:** Операции создания и обновления пользователей теперь используют транзакции для обеспечения атомарности, включая обработку конфликтов по телефону с объединением аккаунтов.
- **Сервис max-bot:** Добавлен новый сервис `max-bot` в docker-compose и workflow развертывания, с рефакторингом команд и middleware для улучшения организации и обработки ошибок.
- **Функциональность переноса UTM-истории:** Реализован метод `TransferUTMHistory` для перемещения UTM-данных между пользователями и метод `DeleteUTMHistoryByUserID` для очистки.

### Изменено
- **Рефакторинг обработки UTM и метаданных:** Поле `MetaData` в модели `User` изменено с `map[string]any` на `map[string]string`, добавлена структурированная модель `Metadata`. UTM-данные теперь извлекаются и сохраняются в базе данных. Индекс UTM-истории упрощен для использования `user_id` и `utm_campaign`.
- **Обновление схемы пользователя:** Поле `TelegramID` помечено как опциональное и устаревшее. Поле `phone` теперь уникально. Добавлена связь с `UserIdentity`. Комментарии схемы переведены на русский язык.
- **Переименование пакетов и директорий:** Пакет `odnoi-krovi-bot` переименован в `odnoi-krovi-max-bot` и `odnoi-krovi-tg-bot`. Директория `bot` переименована в `tg-bot`, с добавлением новой директории `max-bot`.
- **Рефакторинг хендлеров и сервисов:** Хендлеры команд бота реорганизованы для лучшей обработки ошибок. Удален код регистрации пользователя из команд. Добавлена поддержка upsert в схему Ent. Метод `NewDonorPreference` переименован в `DefaultDonorPreference`.
- **Обновление обработки ошибок:** Ошибки Grammy и Bot заменены на `MaxError` в обработчике ошибок.
- **Обновление workflow и Taskfile:** Задача `dev-bot` переименована в `dev-tg-bot`, добавлена `dev-max-bot`. Переменная окружения изменена с `PROD_BOT_API_KEY` на `TG_BOT_TOKEN`.

### Удалено
- **Параметр предпочтений донора из NewUser:** Удален параметр `donorPreference` из функции `NewUser`, добавлен метод `DeleteDonorPreferenceByUserID` в репозиторий.
- **Поле MetaData из модели User:** Поле `MetaData` удалено из модели `User`, логика обработки UTM переработана.

### Технические детали
- В `ent/schema/user.go` добавлены поля `ProviderID`, `ProviderName`, `OriginSource`, связь с `UserIdentity` и `UtmHistory`. Поле `phone` сделано уникальным.
- В `ent/schema/user_identity.go` определена новая схема с полями для провайдера и метаданных.
- В `ent/schema/utm_history.go` добавлена схема с полями UTM-параметров и индексом.
- В `internal/handlers/user_handler.go` добавлен `AuthHandler` и обновлены методы для поддержки транзакций и провайдеров.
- В `internal/repositories/user_repository.go` добавлены методы для работы с UTM-историей, идентичностями и транзакциями.
- В `dto/user.go` обновлены структуры для новых полей, `TelegramID` помечен как опциональный.
- В `cmd/max-bot/` и `cmd/tg-bot/` реорганизованы хендлеры и middleware.
- Обновлены `docker-compose.yml`, `deploy.yml` и `Taskfile.yaml` для поддержки `max-bot`.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent.


## [3.2.5] - 2026-03-04

### Добавлено
- **Поля `PrioritySearch` и `IncludeUnknownBloodGroup` в модель заявки на кровь:** Добавлены новые поля `PrioritySearch` (булево) и `IncludeUnknownBloodGroup` (булево) в модель `BloodSearchRequest` для управления приоритетом поиска и включением питомцев с неизвестной группой крови.

### Изменено
- **Обновлена схема Ent и сгенерированные файлы:** Схема Ent была обновлена для включения новых полей `priority_search` и `include_unknown_blood_group` в сущность `BloodSearchRequest`. Соответствующие сгенерированные файлы Ent были обновлены.
- **Расширены DTO и обработчики для поддержки новых полей:** DTO `BloodSearchRequest` и связанные обработчики были обновлены для корректной обработки и использования новых полей `PrioritySearch` и `IncludeUnknownBloodGroup`.

### Технические детали
- В `ent/schema/bloodsearchrequest.go` добавлены поля `field.Bool("priority_search").Default(false)` и `field.Bool("include_unknown_blood_group").Default(false)`.
- Выполнен `go generate ./ent` для обновления сгенерированных файлов Ent.
- В `dto/blood_search.go` добавлены поля `PrioritySearch bool `json:"prioritySearch"` и `IncludeUnknownBloodGroup bool `json:"includeUnknownBloodGroup"`.
- В `internal/handlers/blood_search_handler.go` и `internal/services/blood_search_service.go` обновлена логика для обработки новых полей при создании и обновлении заявок.


## [3.2.4] - 2026-03-03

### Изменено
- **`DonorPreference` сделан опциональным в DTO пользователя:** Поле `DonorPreference` в DTO `UserDetail` теперь является опциональным.
- **Обновлен маппер пользователя для обработки `nil` `DonorPreference`:** Логика маппинга пользователя была изменена для корректной обработки случаев, когда `DonorPreference` отсутствует.

### Технические детали
- В `dto/user.go` поле `DonorPreference` в структуре `UserDetail` изменено на указатель `*DonorPreference` и добавлен тег `json:",omitempty"`.
- В `internal/mappers/user_mapper.go` обновлена логика маппинга для безопасной обработки `nil` `DonorPreference`.
- Обновлены соответствующие TypeScript модели для отражения опциональности `DonorPreference`.


## [3.2.3] - 2026-03-02

### Добавлено
- **Сущность DonorPreference**: Новая сущность с полями для предпочтительных локаций, периода восстановления, типа компенсации, компенсации такси и частоты уведомлений
- **Связь User ↔ DonorPreference**: Модель User теперь включает one-to-one связь с DonorPreference
- **Поддержка опционального `compensation_type`**: Поле `compensation_type` в схеме теперь nullable
- **Параметр `withDonorPreference`**: Обработчики запросов пользователя принимают флаг для включения данных о предпочтениях донора в ответ

### Изменено
- **Транзакционное обновление пользователя**: Операция обновления пользователя обёрнута в транзакцию для обеспечения атомарности при обновлении данных пользователя и предпочтений донора. Откат при ошибках, коммит только при успешном завершении всех операций
- **Удаление зависимости от file storage**: Удалена зависимость от file storage в обработчиках обновления и мапперах
- **Нормализация URL фотографий перенесена в мапперы**: Логика нормализации URL фотографий перенесена в мапперы и обработчики обновления для возврата временных меток напрямую
- **Удалены URL фотографий из DTO**: Удалены URL фотографий из DTO пользователя и заявки на кровь

### Технические детали
- В схему добавлена новая таблица `donor_preferences` с foreign key на `users`
- В `dto/user.go` добавлена структура `DonorPreference` и обновлена структура `User`
- В `internal/repositories/user_repository.go` упрощены методы, удалены неиспользуемые импорты моделей питомцев
- В `internal/handlers/user_handler.go` обновлены методы для поддержки флага `withDonorPreference`
- В `internal/services/user_service.go` добавлена транзакционная логика для обновления пользователя и предпочтений донора
- В `internal/mappers/` перенесена логика нормализации URL фотографий

## [3.2.0] - 2026-02-01

### Добавлено
- **CQRS архитектура в Application слое**: Разделение на команды (`cmd/`) и запросы (`query/`)
  - `pet/cmd/`: `CreateHandler`, `UpdateHandler`, `DeleteHandler`, `RevalidateDonorHandler`
  - `pet/query/`: `GetByIDHandler`, `GetByUserHandler`
  - `bloodsearch/cmd/`: `CreateRequestHandler`, `UpdateRequestHandler`, `DeleteRequestHandler`, `ApplyForRequestHandler`, `UpdateStatusHandler`
  - `bloodsearch/query/`: `GetByIDHandler`, `GetByPetIDHandler`, `ListRequestsHandler`
  - `file/cmd/`: `GetPresignedURLsHandler`, `ConfirmUploadHandler`
- **Доменные модели для BloodSearch**: `model.BloodRequest`, `model.DonorResponse` с бизнес-логикой
- **Разделение Repository интерфейсов** (Interface Segregation):
  - `PetReadRepository`: `GetByID()`, `GetByUserID()`, `Exists()`
  - `PetWriteRepository`: `Create()`, `Update()`, `Delete()`
  - `PetStatsRepository`: `CountSuitableDonors()`
  - `PetPhotoRepository`: `AddPhotoURLs()`
- **Мапперы** для конвертации: `toDomainModel()` в репозиториях, `mapDTOToBloodRequest()`, `mapBloodRequestToDTO()` в handlers

### Изменено
- **BREAKING**: `BloodRequestRepository` интерфейс работает с доменными моделями вместо ENT типов
  - `Create(ctx, *model.BloodRequest)` вместо `Create(ctx, *ent.CreateBloodSearchRequestInput)`
  - `GetByID()` возвращает `*model.BloodRequest` вместо `*ent.BloodSearchRequest`
  - `Update()` принимает `*model.BloodRequest` вместо `*ent.UpdateBloodSearchRequestInput`
- **BREAKING**: `DonorResponseRepository` возвращает `*model.DonorResponse` вместо `*ent.DonorResponse`
- Репозитории теперь конвертируют ENT ↔ Domain через методы `toDomainModel()`
- HTTP handlers работают с доменными моделями (DTO ↔ Domain маппинг)
- `PetRepository` разделён на специализированные интерфейсы (Read/Write/Stats/Photo)

### Исправлено
- Удалены дубликаты типов `DonorResponseStatus` и `DonorResponse`
- Устранены циклические зависимости между domain слоями
- `LocationRepository` возвращает `[]*model.Location` вместо `[]*ent.Location`
- Используется единый `storage.BuildPhotoURLs()` вместо дублирования логики

### Deprecated
- Старые fat services больше не используются:
  - `domain/pet/pet_service.go` (закомментирован)
  - `domain/bloodsearch/bloodrequest_service.go` (закомментирован)
  - `domain/filestorage/file_service.go` (закомментирован)

### Технический долг
- [ ] Полностью удалить старые сервисы
- [ ] Разделить Pet агрегат на под-агрегаты (Health, Treatment, Analyses)
- [ ] Внедрить Value Objects (Weight, ChipNumber, PhotoGallery)
- [ ] Добавить Domain Events
- [ ] Реализовать Unit of Work

## [3.1.4] - 2026-02-27

### Изменено
- **Замена URL фотографий вместо добавления:** Функциональность обновления URL фотографий теперь заменяет существующие URL новыми, а не добавляет их. Это изменение затрагивает репозитории заявок на кровь и пользователей.

### Технические детали
- В `internal/repositories/blood_search_repository.go` и `internal/repositories/user_repository.go` логика обновления URL фотографий была изменена для перезаписи существующих значений новыми.



## [3.1.3] - 2026-02-26

### Добавлено
- **Эндпоинт для получения всех ограничений доноров:** Добавлен новый эндпоинт для получения всех стоп-факторов и предупреждающих факторов для доноров.
- **Новый обработчик `GetDonorRestrictions`:** Реализован обработчик `GetDonorRestrictions` для обработки запросов на получение ограничений доноров.
- **Новые DTO для ограничений доноров:** Добавлены DTO `DonorRestrictionResponse` и `DonorRestrictionsResponse` для структурированного ответа.
- **Доменная функция для получения факторов:** В доменной модели добавлена функция для извлечения стоп-факторов и предупреждающих факторов.

### Технические детали
- В `internal/handlers/donor_handler.go` (или аналогичном файле) добавлен метод `GetDonorRestrictions`.
- В `dto/donor.go` (или аналогичном файле) добавлены структуры `DonorRestrictionResponse` и `DonorRestrictionsResponse`.
- В `internal/domain/donor.go` (или аналогичном файле) добавлена функция для получения факторов.
- Обновлены маршруты API для включения нового эндпоинта.


## [3.1.2] - 2026-02-25

### Добавлено
- **Количество подходящих доноров в DTO заявки на кровь:** Добавлено новое поле `SuitableDonors` в DTO `BloodSearchPetRequest`, которое отслеживает количество подходящих доноров для заявки на кровь. Это поле заполняется путем запроса к базе данных для доноров, соответствующих критериям группы крови и не имеющих стоп-факторов. Метод `GetRequestByPetID` в сервисном слое теперь возвращает это количество вместе с заявкой на кровь.
- **Ответы доноров в DTO заявки на кровь:** Включены детали доноров, такие как имя, фотографии, группа крови, в ответы. Рассчитывается безопасное количество донации на основе типа и веса питомца. Обновлен репозиторий для загрузки данных донора и группы крови с ответами.

### Изменено
- **Рефакторинг DTO и обработчиков заявок на кровь и питомцев:**
    - `BloodSearchPetRequest` переименован в `BloodSearchRequest`.
    - Добавлен DTO `CreateBloodSearchRequest` для создания.
    - Обновлен обработчик питомцев для возврата упрощенного ответа на создание.
    - Различные поля DTO сделаны опциональными с `omitempty`.
    - Добавлено новое значение перечисления статуса питомца `blood_found`.
- **Замена `donor_restrictions` на `stop_factors` и `warn_factors`:** Поле `donor_restrictions` заменено на отдельные поля `stop_factors` и `warn_factors`.

### Технические детали
- В `dto/blood_search.go` добавлено поле `SuitableDonors` в `BloodSearchPetRequest`.
- В `internal/services/blood_search_service.go` метод `GetRequestByPetID` обновлен для возврата количества подходящих доноров.
- В `dto/blood_search.go` и `internal/handlers/blood_search_handler.go` `BloodSearchPetRequest` переименован в `BloodSearchRequest`.
- Добавлен `dto/create_blood_search_request.go`.
- В `internal/handlers/pet_handler.go` обновлен ответ на создание.
- В различных DTO добавлены теги `json:",omitempty"`.
- В `internal/domain/pet.go` (или аналогичном файле) добавлено значение `blood_found` в перечисление статусов питомца.
- В схеме `Pet` (или соответствующей) поля `donor_restrictions` заменены на `stop_factors` и `warn_factors`.
- В `dto/blood_search.go` и `internal/repositories/blood_search_repository.go` обновлены структуры для включения деталей доноров и расчета безопасного количества донации.



## [3.1.1] - 2026-02-24

### Добавлено
- **Функциональность обновления заявки на кровь:** Реализована возможность обновления существующих заявок на кровь.

### Изменено
- **Обновление хендлера, репозитория и сервиса заявок на кровь:**
    - Хендлер теперь обрабатывает запросы на обновление заявок.
    - Репозиторий возвращает обновленную сущность заявки на кровь.
    - Сервис передает обновленную сущность от репозитория к хендлеру.

### Технические детали
- В `internal/handlers/blood_search_handler.go` добавлен метод для обработки запросов на обновление заявок на кровь.
- В `internal/repositories/blood_search_repository.go` метод обновления заявки теперь возвращает обновленную сущность.
- В `internal/services/blood_search_service.go` обновлен метод для обработки обновления заявок и возврата обновленной сущности.

## [3.1.0] - 2026-02-24

### Добавлено
- **Доменная модель питомца:** Введена доменная модель питомца, которая теперь функционирует как самостоятельная сущность. Бизнес-логика, включая проверки стоп-факторов, теперь инкапсулирована в самой доменной модели.
- **Хендлер для отклика на заявку на кровь:** Создан новый хендлер для обработки откликов на заявки на кровь.

### Изменено
- **Рефакторинг хендлера, репозитория и сервиса питомца:** Хендлер, репозиторий и сервис питомца были переписаны для интеграции с новой доменной моделью питомца.
- **Рефакторинг `BloodSearchRequest`:** Проведен небольшой рефакторинг `BloodSearchRequest` в сервисе поиска крови для улучшения читаемости и поддержки новой функциональности.

### Технические детали
- В `internal/domain/pet.go` определена новая доменная модель питомца с инкапсулированной бизнес-логикой.
- В `internal/handlers/pet_handler.go`, `internal/repositories/pet_repository.go` и `internal/services/pet_service.go` обновлены реализации для использования доменной модели питомца.
- В `internal/services/blood_search_service.go` внесены изменения, связанные с `BloodSearchRequest`.
- В `internal/handlers/blood_response_handler.go` (или аналогичном файле) добавлен новый хендлер для обработки откликов на заявки на кровь.


## [3.0.13] - 2026-02-21

### Изменено
- **Обновлены значения перечисления `onBoarding` в `BloodSearchPetRequest`:** Значения перечисления для поля `onBoarding` в `BloodSearchPetRequest` были изменены с "DONOR,RECIPIENT" на "SEARCH,BLOOD_CARD".

### Технические детали
- В `dto/blood_search.go` обновлены допустимые значения для поля `onBoarding` в структуре `BloodSearchPetRequest`.
- Обновлены соответствующие DTO и валидация для отражения новых значений перечисления.


## [3.0.12] - 2026-02-20

### Изменено
- **Рефакторинг метода `buildFullPhotoURLs` в `BloodSearchService`:** Метод `buildFullPhotoURLs` переименован в `BuildFullPhotoURLs` и теперь принимает указатель на `BloodSearchRequest`. Он модифицирует объект `BloodSearchRequest` напрямую, обновляя его `photo_urls` и добавляя параметр запроса `?t=` с временной меткой к каждому URL. Это обеспечивает инвалидацию кэша и упрощает использование метода.

### Технические детали
- В `internal/services/blood_search_service.go` метод `buildFullPhotoURLs` был переименован в `BuildFullPhotoURLs` и изменен для прямого обновления поля `photo_urls` в объекте `BloodSearchRequest` и добавления временной метки к каждому URL.
- Обновлены все вызовы `BuildFullPhotoURLs` в `BloodSearchService` для соответствия новой сигнатуре.

## [3.0.11] - 2026-02-19 - Обновление версии API до 3.0.11 и опциональность ID в BloodSearchPetRequest

Этот релиз включает обновление версии API до 3.0.11 и делает поле `ID` в структуре `BloodSearchPetRequest` опциональным.

### Изменено
- **Обновлена версия API:** Версия API обновлена до 3.0.11.
- **Поле `ID` в `BloodSearchPetRequest` стало опциональным:** Поле `ID` в `BloodSearchPetRequest` теперь является опциональным, что повышает гибкость при работе с запросами.

### Технические детали
- В конфигурации Huma API обновлена версия до 3.0.11.
- В `dto/blood_search.go` для `BloodSearchPetRequest` поле `ID` изменено на указатель и добавлен тег `json:",omitempty"`.
- Обновлены соответствующие TypeScript интерфейсы для отражения опциональности поля `ID`.


## [3.0.10] - 2026-02-19 - Удаление обязательной валидации для ID в BloodSearchPetRequest

Этот релиз включает изменение валидации для поля `ID` в `BloodSearchPetRequest`, делая его необязательным.

### Изменено
- **Удалена обязательная валидация для `ID` в `BloodSearchPetRequest`:** Поле `ID` в `BloodSearchPetRequest` теперь не является обязательным для валидации.

### Технические детали
- В `dto/blood_search.go` удалена обязательная валидация для поля `ID` в структуре `BloodSearchPetRequest`.

## [3.0.9] - 2026-02-19 - Обновление DTO для BloodSearchPetRequest

Этот релиз включает обновление DTO `BloodSearchPetRequest` для использования `omitempty` и полей-указателей для опциональных значений, что повышает гибкость и консистентность при работе с данными.

### Изменено
- **Обновление DTO `BloodSearchPetRequest`:** В `BloodSearchPetRequest` опциональные поля теперь используют `omitempty` и являются полями-указателями.

### Технические детали
- В `dto/blood_search.go` для `BloodSearchPetRequest` добавлены теги `json:",omitempty"` и поля изменены на указатели для опциональных значений.


## [3.0.8] - 2026-02-18 - Добавление полей времени и онбординга донора в BloodSearchPetRequest

Этот релиз расширяет структуру `BloodSearchPetRequest` полями `createdAt`, `updatedAt` и `deletedAt` для улучшения отслеживания жизненного цикла заявок. Также добавлено перечисление для онбординга донора и обновлены соответствующие обработчики и определения API.

### Добавлено
- **Поля времени в `BloodSearchPetRequest`:** Добавлены поля `createdAt`, `updatedAt` и `deletedAt` в `BloodSearchPetRequest` для более полного отслеживания состояния заявок.
- **Перечисление онбординга донора:** Введено перечисление для онбординга донора, что позволяет более гибко управлять статусами доноров.
- **Обновление обработчиков и API:** Соответствующие обработчики и определения API были обновлены для поддержки новых полей и перечисления.
- **Мок-данные для питомцев-доноров:** В пакет моков добавлены мок-данные для питомцев-доноров.

### Технические детали
- В `dto/blood_search.go` добавлены поля `CreatedAt`, `UpdatedAt`, `DeletedAt` к `BloodSearchPetRequest`.
- Обновлены схемы Ent и DTO для включения нового перечисления онбординга донора.
- Мок-данные в `mocks/` обновлены для отражения изменений.

## [3.0.7] - 2026-02-18 - Эндпоинт для получения заявки на кровь по ID питомца

Этот релиз добавляет новый эндпоинт для получения заявок на кровь, связанных с определенным питомцем, что упрощает доступ к информации о заявках.

### Добавлено
- **Эндпоинт для получения заявки на кровь по ID питомца:** Добавлен новый эндпоинт, который позволяет получать заявки на кровь, используя ID питомца.

### Технические детали
- В `internal/handlers/blood_search_handler.go` добавлен метод для обработки запросов по ID питомца.
- Обновлены маршруты API для включения нового эндпоинта.

## [3.0.6] - 2026-02-18 - Добавление поля onBoarding и использование входных типов для заявок на кровь

Этот релиз включает добавление поля `onBoarding` в схему и DTO заявок на кровь, а также рефакторинг репозитория и сервиса для использования входных типов (`CreateBloodSearchRequestInput` и `UpdateBloodSearchRequestInput`), что повышает консистентность и упрощает работу с данными.

### Добавлено
- **Поле `onBoarding` в схему и DTO заявок на кровь:** Добавлено новое поле `onBoarding` для отслеживания статуса онбординга в заявках на кровь.
- **GraphQL мутации для заявок на кровь:** Добавлены GraphQL мутации для создания и обновления заявок на кровь.

### Изменено
- **Использование входных типов для репозитория и сервиса:** Репозиторий и сервис были обновлены для использования `CreateBloodSearchRequestInput` и `UpdateBloodSearchRequestInput`, что обеспечивает более структурированный подход к обработке входных данных.
- **Замена ручного маппинга на `copier`:** Ручной маппинг данных в обработчике заменен на использование библиотеки `copier` для автоматизации процесса.

### Технические детали
- В `schema/blood_search_request.go` и `dto/blood_search.go` добавлено поле `onBoarding`.
- В `internal/repositories/blood_search_repository.go` и `internal/services/blood_search_service.go` обновлены методы для использования входных типов.
- В `internal/handlers/blood_search_handler.go` реализовано использование `copier` для маппинга.
- Добавлены соответствующие GraphQL схемы и резолверы.

## [3.0.5] - 2026-02-16 - Обновление значений перечисления статуса онбординга

Этот релиз включает обновление значений перечисления для статуса онбординга в структурах `User` и `UserUpdate`.

### Изменено
- **Обновлены значения перечисления статуса онбординга:** Значения перечисления для статуса онбординга были изменены с "DONOR,RECIPIENT" на "START,FIND_BLOOD" в структурах `User` и `UserUpdate`.

### Технические детали
- В `dto/user.go` и `schema/user.go` обновлены допустимые значения для поля `on_boarding`.
- Обновлены соответствующие DTO и валидация для отражения новых значений перечисления.

## [3.0.4] - 2026-02-16 - Рефакторинг поиска групп крови для использования только группы крови

Этот релиз включает рефакторинг методов поиска групп крови, чтобы они зависели только от самой группы крови, исключая зависимость от типа питомца. Это упрощает логику и делает систему более гибкой.

### Изменено
- **Удалена зависимость от типа питомца в поиске групп крови:** Методы поиска групп крови теперь используют только значение группы крови, без учета типа питомца.
- **Упрощена логика поиска групп крови:** Логика, связанная с поиском и управлением группами крови, была упрощена за счет удаления ненужной проверки типа питомца.

### Технические детали
- **Обновлены репозитории и сервисы:** Методы в репозиториях и сервисах, отвечающие за поиск групп крови, были изменены для работы исключительно с `BloodGroup` без учета `PetType`.
- **Удалены соответствующие параметры и условия:** Все параметры и условные операторы, связанные с `PetType` в контексте поиска групп крови, были удалены.


## [3.0.3] - 2026-02-15 - Рефакторинг отношения "Питомец-Группа Крови" на One-to-Many

Этот релиз включает рефакторинг отношения между питомцами и группами крови, переводя его из One-to-One в One-to-Many. Это изменение позволяет нескольким питомцам иметь одну и ту же группу крови, что является более реалистичной моделью данных и упрощает управление группами крови для множества питомцев.

### Изменено
- **Отношение "Питомец-Группа Крови" изменено на One-to-Many:** Теперь одна группа крови может быть связана с несколькими питомцами. Каждый питомец по-прежнему имеет только одну группу крови. Это изменение позволяет эффективно управлять группами крови, которые могут быть общими для многих питомцев.
- **Обновлены хендлеры и сервисы:** Логика работы с группами крови в хендлерах и сервисах была адаптирована для соответствия новому One-to-Many отношению.

### Технические детали
- **Обновление схемы базы данных:** В схеме Ent (`Pet`) отношение к `BloodGroup` было изменено с `edge.Ref` на `edge.To` без `Unique()`, что обеспечивает One-to-Many связь.
- **Рефакторинг репозиториев:** Методы репозиториев, работающие с группами крови питомцев, были обновлены для отражения нового отношения.
- **Рефакторинг сервисов и хендлеров:** Логика обработки запросов и ответов в сервисах и хендлерах была обновлена с учетом One-to-Many отношения для групп крови.
- **Обновление DTO:** Все DTO были синхронизированы с новыми изменениями в схеме.


## [3.0.2] - 2026-02-15 - Рефакторинг отношения "Питомец-Группа Крови" на One-to-One

Этот релиз включает рефакторинг отношения между питомцами и группами крови, переводя его из Many-to-Many в One-to-One. Это изменение упрощает модель данных, повышает консистентность и оптимизирует работу с группами крови питомцев.

### Изменено
- **Отношение "Питомец-Группа Крови" изменено на One-to-One:** Теперь каждый питомец может иметь только одну группу крови, и каждая группа крови может быть связана только с одним питомцем. Это изменение значительно упрощает запросы и мутации, связанные с группами крови.
- **Удалены избыточные поля группы крови:** Все поля, связанные с группами крови, которые дублировались или были избыточными в контексте нового отношения One-to-One, были удалены из схемы питомца.
- **Обновлены хендлеры и сервисы:** Логика работы с группами крови в хендлерах и сервисах была адаптирована для соответствия новому One-to-One отношению.

### Технические детали
- **Обновление схемы базы данных:** В схеме Ent (`Pet`) отношение к `BloodGroup` было изменено с `edge.To` на `edge.Ref` с `Unique()`, что обеспечивает One-to-One связь.
- **Рефакторинг репозиториев:** Методы репозиториев, работающие с группами крови питомцев, были обновлены для отражения нового отношения.
- **Рефакторинг сервисов и хендлеров:** Логика обработки запросов и ответов в сервисах и хендлерах была обновлена с учетом One-to-One отношения для групп крови.
- **Обновление DTO:** Все DTO были синхронизированы с новыми изменениями в схеме.


## [3.0.1] - 2026-02-14 - Глобальный Переход на String ID и Рефакторинг

Этот релиз представляет собой **масштабный рефакторинг**, связанный с переходом всех идентификаторов сущностей в базе данных на строковый тип. Это изменение коснулось практически всех слоев приложения и направлено на повышение гибкости и консистентности данных.

### Изменено
- **Глобальный переход на String ID:** Все числовые ID в базе данных и соответствующих моделях (локации, породы, группы крови, компоненты крови и т.д.) были изменены на строковые. Это повлекло за собой значительную переработку схемы базы данных и логики работы с идентификаторами.
- **Масштабный рефакторинг структур User и Pet:** Для обеспечения консистентности с новыми строковыми ID, структуры `User` и `Pet`, а также связанные с ними DTO, хендлеры, сервисы и репозитории были полностью переработаны.
- **Обновление данных через GraphQL-подобный API Ent:** Обновление данных теперь осуществляется с использованием удобного плагина для Ent, который предоставляет GraphQL-подобный API для мутаций, упрощая маппинг и взаимодействие с фронтендом.
- **Исправление ошибок фронтенда, связанных с ID:** Из-за смены типов ID на строковые, на фронтенде возникли ошибки, связанные с некорректной обработкой словарей (dictionaries) и маппингом. Эти ошибки были исправлены.
- **Корректное отображение пород:** Исправлена логика отображения пород, которая могла быть нарушена из-за изменений в типах ID.

### Технические детали
- **Обновление схемы базы данных:** Все поля ID в схемах Ent (`Location`, `Breed`, `BloodGroup`, `BloodComponent` и других) были изменены с `field.Int()` на `field.String()`.
- **Рефакторинг репозиториев:** Все методы репозиториев, работающие с ID, были адаптированы для строковых идентификаторов.
- **Рефакторинг сервисов и хендлеров:** Логика обработки запросов и ответов в сервисах и хендлерах была обновлена с учетом строковых ID.
- **Интеграция GraphQL-подобного API Ent:** Внедрена новая система для выполнения мутаций, использующая возможности Ent для более декларативного и гибкого обновления данных.
- **Обновление DTO:** Все DTO были синхронизированы с новыми строковыми ID.
- **Исправление фронтенд-логики:** Проведены необходимые изменения на фронтенде для корректной работы со строковыми ID в словарях и других структурах данных.

## [2.3.1] - 2026-02-13

### Исправлено
- **Исправлена логика обновления отношений питомца:** Условие для обновления отношений питомца в обработчике `UpdatePet` теперь корректно проверяет наличие анализов и перезаписывает их.


## [2.3.0] - 2026-02-13

### Добавлено
- **Поддержка опционального `on_boarding` для пользователя:** Поле `on_boarding` в схеме пользователя теперь опционально, что позволяет очищать и запрашивать нулевые значения.

### Изменено
- **Рефакторинг обработчиков питомцев для использования жадной загрузки:** Обработчики питомцев теперь используют запросы с жадной загрузкой для более эффективного получения связанных данных.
- **Рефакторинг методов запросов пользователя для жадной загрузки:** Переименован `GetQuery` в `GetQueryByID` и добавлен `GetQueryByTelegram` для ясности и согласованности при обработке запросов с жадной загрузкой по ID и Telegram ID.
- **Рефакторинг обработки пользователя для использования запросов с жадной загрузкой:** Прямое получение пользователя заменено на подход, основанный на запросах, для большей гибкости. Обработка URL фотографий теперь использует публичный метод для согласованности.
- **Рефакторинг обновления питомца для использования Ent mutation API:**
    - Удалено поле `photoUrls` из DTO `PetUpdate`.
    - Обновления на основе карты заменены прямыми мутациями Ent.
    - Обновления питомца и его связей разделены на отдельные операции.
    - Метод `UpdatePet` в сервисном слое переименован в `UpdatePetRelations`.
    - Улучшена обработка ошибок и логирование.
- **Удален устаревший `UpdateUserProfile` из интерфейса `UserService`.**
- **Добавлен репозиторий местоположений в обработчик пользователя и рефакторинг обновлений пользователя:**
    - Логика обновления пользователя перемещена из сервиса в обработчик с использованием мутаций Entgo.
    - Удалена устаревшая вспомогательная функция `toUpdatesMap`.
    - Метод сервиса `UpdateUserProfile` помечен как устаревший.
- **Изменен тип поля `on_boarding` с `bool` на `[]string`:** Поле `on_boarding` в схеме пользователя теперь хранит массив строк вместо булевого значения. Это изменение затрагивает схему, мутации и связанные DTO.

### Удалено
- `photoUrls` из модели `PetUpdate`.

### Технические детали
- В схеме пользователя поле `on_boarding` сделано опциональным.
- Обновлены конечные точки API пользователя и типы.
- Исправлен путь и параметры конечной точки пользователя Telegram.
- Методы запросов пользователя (`GetQuery`, `GetQueryByID`, `GetQueryByTelegram`) были реорганизованы.
- Логика обновления питомца в `internal/services/pet_service.go` и `internal/handlers/pet_handler.go` была переработана для использования Ent mutation API.
- Удален метод `UpdateUserProfile` из `internal/services/user_service.go`.
- В `internal/handlers/user_handler.go` добавлена зависимость от репозитория местоположений, и логика обновления пользователя была изменена для использования мутаций Ent.
- Тип поля `on_boarding` в `schema/user.go` изменен на `[]string`.

## [2.2.5] - 2026-02-10

### Изменено
- **Рефакторинг метода `BuildFullPhotoURLs`:** Метод `BuildFullPhotoURLs` теперь модифицирует объект питомца напрямую, обновляя его `photo_urls` и добавляя параметр запроса `?t=` с временной меткой к каждому URL. Это обеспечивает инвалидацию кэша и упрощает использование метода.

### Технические детали
- В `internal/services/pet_service.go` метод `BuildFullPhotoURLs` был изменен для прямого обновления поля `photo_urls` в объекте `Pet` и добавления временной метки к каждому URL.

## [2.2.4] - 2026-02-10

### Изменено
- **Обновление фотографий питомцев:** Прямое обновление URL фотографий заменено на обновление через полную сущность питомца, что обеспечивает консистентность данных.
- **Удален метод обновления аватара питомца:** Устаревший метод `UpdatePetAvatar`, который обрабатывал обновления аватара отдельно, был удален.

### Технические детали
- В `internal/services/pet_service.go` и `internal/handlers/pet_handler.go` логика обновления фотографий теперь интегрирована в общий метод обновления питомца.
- Удален метод `UpdatePetAvatar` из `internal/services/pet_service.go` и `internal/repositories/pet_repository.go`, а также все его вызовы.


## [2.2.3] - 2026-02-10

### Изменено
- **Обработка анализов питомцев:** Логика обновления анализов питомцев изменена для явной проверки на `nil` вместо проверки длины, что делает обработку более точной.

### Технические детали
- В `internal/services/pet_service.go` обновлена логика обработки анализов для проверки на `nil` вместо длины.

## [2.2.2] - 2026-02-06

### Добавлено
- **Интерцептор для логирования медленных запросов к БД:** Добавлен интерцептор запросов к базе данных для логирования запросов, выполнение которых занимает больше заданного порогового значения времени.

### Изменено
- **Генерация ID:** Функция `generateID` теперь использует настраиваемый набор символов для генерации `nanoid`, что позволяет создавать более специфичные и предсказуемые идентификаторы.
- **Переименование импорта логгера:** Импорт `log` переименован в `charmlog` для ясности и предотвращения конфликтов с другими пакетами.
- **Рефакторинг настройки сервера:** Настройка сервера теперь использует метод `Use` для добавления middleware, что улучшает читаемость и модульность.
- **Рефакторинг инициализации API и команда OpenAPI:**
    - Удален флаг генерации документации, и эта функциональность перенесена в команду Cobra.
    - `SetupLogger` переименован в `SetupSlogDefaultLogger`.
    - `SetupCORS` теперь напрямую возвращает функцию обработчика.
    - Увеличено время ожидания завершения работы сервера с 5 до 30 секунд.
    - Генерация OpenAPI перенесена в отдельную команду Cobra.
    - Оптимизирована организация `main.go` и импортов.
- **Замена кастомного middleware на `slog-http`:** Кастомный пакет middleware был удален и заменен на `slog-http` для логирования и восстановления после падений.

### Технические детали
- В `internal/interceptors/db_logger.go` добавлен новый интерцептор для логирования медленных запросов.
- В `internal/utils/id_generator.go` обновлена функция `generateID` для использования кастомного набора символов.
- В `cmd/api/main.go` и других файлах, использующих логгер, импорт `log` заменен на `charmlog`.
- В `cmd/api/main.go` и `internal/server/server.go` обновлена логика настройки сервера для использования `middleware.Use`.
- В `cmd/api/main.go` и `cmd/api/cobra.go` реорганизована инициализация API и добавлена команда OpenAPI.
- Удален `internal/middleware` и интегрирован `slog-http` в `internal/server/server.go`.

## [2.2.1] - 2026-02-05

### Изменено
- **Валидация длины номера чипа:** Поле `chip_number` в DTO `Pet` теперь валидируется на точную длину в 15 символов, если оно присутствует.
- **Исправлена передача `chip_number` по ссылке:** Обработка `chip_number` в `EntPetRepository` изменена для передачи по ссылке, что обеспечивает корректное обновление.

### Технические детали
- В `dto/pet.go` добавлена валидация `len=15` для поля `ChipNumber`.
- В `internal/repositories/ent_pet_repository.go` обновлена логика для передачи `chip_number` по ссылке при обновлении.

## [2.2.0] - 2026-02-04

### Изменено
- **Рефакторинг обработки здоровья и лечения питомцев:** Логика обновления здоровья и лечения питомцев изменена для явной обработки nil-значений. Объекты здоровья и лечения теперь полностью перезаписываются при обновлении, что требует возврата полного объекта с фронтенда, включая предыдущие записи и новые изменения.
- **Замена истории анализов на подход удаления и создания:** История анализов заменена на подход удаления и создания для обеспечения консистентности. Теперь у питомца может быть только один анализ определенного типа, без хранения исторических записей.

### Технические детали
- Обновлена логика в сервисах и обработчиках питомцев для полной перезаписи объектов здоровья и лечения при обновлении.
- В репозиториях анализов реализован подход удаления существующих записей перед созданием новых для каждого типа анализа.
```


## [2.1.6] - 2026-02-04

### Добавлено
- **Поддержка транзакций для операций с заявками на кровь и обновлениями статуса питомцев:** Добавлены транзакционные методы в репозитории и обновлен сервис поиска крови для обработки транзакций при создании, обновлении и удалении. Статус питомца теперь обновляется в рамках той же транзакции, что и операции с заявками на кровь.
- **Поле статуса в BloodSearchPetRequest:** Добавлено поле `Status` в структуру `BloodSearchPetRequest` для отслеживания статуса заявок.

### Изменено
- **Рефакторинг DTO и обработчиков заявок на кровь:** Переименованы `BloodSearchRequestResponse` в `BloodRequestResponse` и `BloodSearchRequestsResponse` в `BloodRequestsResponse`. Обновлены перечисления статусов в моделях TypeScript. Удалены неиспользуемые обертки ответов в обработчиках.
- **Обновление логики статуса питомца:** Статус питомца устанавливается в 'donor' при отсутствии ограничений.
- **Изменение метода валидации доноров с GET на POST:** Эндпоинт валидации доноров теперь использует POST вместо GET для лучшего отражения побочных эффектов. Факторы валидации пересчитываются и сохраняются, ответ включает обновленные факторы. Факторы дедуплицируются и категоризируются по префиксу (STOP_ или WARN_).
- **Добавление поля ограничений доноров в запрос обновления питомца:** Включено поле ограничений доноров в запросы обновления питомцев.
- **Добавление валидатора доноров в PetHandler:** Обновлен PetHandler для включения валидатора доноров. Модель DonorRestrictions упрощена путем удаления избыточных примеров. Добавлены новые модели RestrictionFactor и DonorRestrictions в общем клиенте TypeScript.
- **Введение единого поля ограничений доноров:** Заменены отдельные поля donor_stop_factors и donor_warn_factors на единое поле donor_restrictions для упрощения модели данных. Обновлены все связанные схемы, мутации и запросы.
- **Удаление URL фотографий из DTO питомцев:** Удалено поле PhotoURLs из структур PetCreate и Pet для упрощения модели данных питомцев.

### Технические детали
- В репозиториях добавлены методы с поддержкой транзакций для операций с заявками на кровь и статусами питомцев.
- Обновлены DTO заявок на кровь для переименования структур ответов и добавления поля статуса.
- Изменен эндпоинт валидации доноров на POST с логикой пересчета и сохранения факторов.
- Интегрирован валидатор доноров в PetHandler с новыми моделями ограничений.
- Объединены поля факторов остановки и предупреждений доноров в единое JSON-поле в модели питомца.
- Удалены ссылки на URL фотографий из DTO питомцев для упрощения структуры.
```


## [2.1.5] - 2026-02-03

### Добавлено
- **Валидация доноров в сервисе питомцев:** Введена логика валидации доноров в сервисе питомцев, включая новый метод `ApplyValidation`. Интерфейс `PetValidator` переименован в `DonorValidator` для лучшего отражения его назначения. Результаты валидации теперь применяются к объектам питомцев и сохраняются.
- **Факторы остановки и предупреждения доноров в модели питомца:** Добавлены JSON-поля для хранения факторов остановки и предупреждения доноров. Реализован валидатор питомцев с проверками факторов остановки и предупреждения по умолчанию. Добавлены правила валидации для различных условий здоровья и донаций питомцев.

### Изменено
- **Обновление сервиса питомцев:** Сервис питомцев обновлен для использования репозитория запросов крови вместо сервиса поиска крови.

### Технические детали
- В `internal/services/pet_service.go` добавлен новый метод `ApplyValidation` для применения результатов валидации к объектам питомцев.
- Интерфейс `PetValidator` переименован в `DonorValidator` в соответствующих файлах.
- В модели `Pet` добавлены поля для хранения факторов остановки и предупреждения доноров в формате JSON.
- Обновлены зависимости сервиса питомцев для интеграции с репозиторием запросов крови.
```

## [2.1.4] - 2026-01-30

### Добавлено
- **Каскадное удаление заявок на поиск крови:** При удалении питомца теперь автоматически удаляются все связанные с ним активные заявки на поиск крови.

### Технические детали
- В `internal/services/pet_service.go` в `DeletePet` добавлена логика для получения и удаления связанных заявок через `BloodSearchService`.
- В `internal/services/pet_service.go` в `PetServiceImpl` добавлен `BloodSearchService` как зависимость.
- В `cmd/api/main.go` обновлен конструктор `NewPetService` для передачи `BloodSearchService`.
- В `internal/services/pet_service.go` обновлено логирование ошибок при удалении заявок с `slog.WarnContext`.

## [2.1.3] - 2026-01-29

### Изменено
- **Перемещение поля репродуктивного статуса:** Поле репродуктивного статуса перемещено из сущности здоровья питомца (PetHealth) в основную сущность питомца (Pet).

### Технические детали
- В `schema/pet.go` добавлено поле репродуктивного статуса и обновлены миграции.
- В `schema/pethealth.go` удалено поле репродуктивного статуса.
- Обновлены с
- Обновлены соответствующие DTO, обработчики и сервисы для работы с новым расположением поля.
- Скорректированы ссылки внешних ключей в схеме `Pet` для корректного отражения изменений.


## [2.1.2] - 2026-01-27

### Удалено
- **Удалены поля возраста из схемы Pet:** Поля `age_years` и `age_months` были удалены из схемы `Pet`.
- **Удален связанный код возраста:** Весь связанный код, включая мутации, запросы и DTO, относящийся к полям `age_years` и `age_months`, был удален.

### Изменено
- **Упрощена схема Pet:** Схема `Pet` теперь полагается исключительно на поле `birth_date` для всех расчетов возраста, что упрощает структуру и устраняет избыточные поля.

### Технические детали
- В `schema/pet.go` удалены поля `age_years` и `age_months`.
- Обновлены все DTO, мутации и запросы, затрагивающие модель `Pet`, для удаления ссылок на `age_years` и `age_months`.
- Логика расчета возраста теперь полностью основана на `birth_date`.


## [2.1.1] - 2026-01-27

### Изменено
- **Исправлена опечатка в предзагрузке репозитория питомцев:** Изменено "Analysis" на "Analyses" для корректной предзагрузки связанных анализов.
- **Реорганизованы методы сервиса:** Методы `buildFullPhotoURLs` перемещены после `ConfirmPhotos` в соответствующих сервисах для улучшения логической последовательности.
- **Метод `calculateAgeFields` стал методом `PetService`:** Метод `calculateAgeFields` теперь является частью `PetService` и вызывается при создании питомца для автоматического расчета возраста.

### Технические детали
- В `pet_repository.go` изменено имя связи для предзагрузки с `Analysis` на `Analyses`.
- В `s3_service.go` и других сервисах, использующих S3, методы `buildFullPhotoURLs` перемещены после `ConfirmPhotos`.
- Метод `calculateAgeFields` интегрирован в `pet_service.go` как приватный метод `(s *PetService) calculateAgeFields(...)` и вызывается в методе `CreatePet`.


## [2.1.0] - 2026-01-20

### Добавлено
- **Поддержка S3 для заявок на кровь (Blood Requests):** Теперь S3 хранилище поддерживает загрузку и подтверждение фотографий для сущности `Blood Request`.
- **Массив `photo_urls`:** В схемы `User`, `Pet` и `BloodRequest` добавлено новое поле `photo_urls` (массив строк) для хранения ссылок на фотографии.
- **Год в путях S3:** В пути объектов S3 для `User`, `Pet` и `BloodRequest` добавлен текущий год для лучшей организации хранения.

### Изменено
- **Переработана логика S3 хранилища:**
    - S3 теперь принимает количество фотографий и тип сущности (`User`, `Pet`, `BloodRequest`).
    - Возвращает соответствующее количество предзаполненных ссылок для загрузки фотографий.
    - Подтверждение загрузки фотографии также переработано и принимает ID сущности и путь подтверждения.
- **Обновлены схемы Ent:** Схемы `User`, `Pet` и `BloodRequest` обновлены для включения поля `photo_urls`.
- **Поддержка `Blood Request ID`:** Добавлена поддержка `Blood Request ID` как для предзаполненных URL, так и для URL аватаров.

### Технические детали
- В `schema/user.go`, `schema/pet.go` и `schema/bloodrequest.go` добавлено поле `photo_urls` типа `[]string`.
- Логика генерации предзаполненных URL и подтверждения загрузки в S3 сервисе обновлена для учета типа сущности и добавления года в путь.
- Обновлены соответствующие DTO и обработчики для работы с новым полем `photo_urls` и измененной логикой S3.



## [2.0.7] - 2026-01-17

### Добавлено
- Добавлен DTO `PetAnalysisGroup` для категоризации анализов питомцев по типу.
- Обновлен обработчик питомцев для группировки анализов в ответе по типу.
- Обновлена документация API и экспорты моделей

### Технические детали
- В `dto/pet.go` добавлен `PetAnalysisGroup` для среза `PetAnalysis`.
- В `handler/pet_handler.go` реализована логика группировки анализов по `AnalysisName` перед отправкой ответа.
- BreedID - теперь не обязательный параметр.
- Обновлена спецификация OpenAPI для отражения новой структуры ответа с группировкой анализов.




## [2.0.6] - 2026-01-16

### Изменено
- Поля `ID`, `CreatedAt`, `UpdatedAt`, `DeletedAt` в DTO (Data Transfer Objects) теперь явно помечены как `readOnly` для корректного отображения в документации OpenAPI.
- Ответы сервера, содержащие списки питомцев, теперь всегда сортируются по полю `ID` в порядке возрастания, обеспечивая предсказуемый порядок данных.

### Технические детали
- В репозитории, ответственном за получение списка питомцев, реализована логика сортировки по `ID` в порядке возрастания (например, с использованием `ORDER BY id ASC` в SQL-запросах).



## [2.0.5] - 2026-01-15

### Изменено
- Обновлены DTO для использования `omitempty` и полей-указателей для опциональных значений.
- В `BloodSearchPetRequest` опциональные поля теперь используют `omitempty`.
- Удалена структура `PetCreate`, теперь для создания питомца используется сама структура `Pet`.
- В DTO `Pet` и `User` поля временни (`CreatedAt`, `UpdatedAt`, `DeletedAt`) теперь являются указателями.
- Синхронизированы TypeScript модели с изменениями бэкенда.

### Технические детали
- Добавлена задача `generate-swagger` в команду `dev backend` для автоматической генерации документации.
- В `schema/pet.go` и `schema/user.go` поля `CreatedAt`, `UpdatedAt`, `DeletedAt` изменены на `*time.Time`.
- В `dto/blood_search.go` для `BloodSearchPetRequest` добавлены теги `json:",omitempty"` к опциональным полям.
- Удален файл `dto/pet_create.go` и все ссылки на `PetCreate`.
- Обновлены все места использования DTO `Pet` и `User` для корректной работы с полями-указателями и `omitempty`.
- Перегенерированы TypeScript модели для соответствия новым DTO.



## [2.0.3] - 2026-01-14

### Изменено
- Обновлена схема Pet: поле `analyses` теперь ссылается на `PetAnalysis` без `Unique()` ограничения.
- Скорректирована схема `PetAnalysis`: поле `pet_id` теперь является обязательным (`Required()`).
- В схеме `PetAnalysis` поле `analysis_date` теперь может быть `Nillable()`.

### Технические детали
- В `schema/pet.go` для `edge.From("analyses", PetAnalysis.Type).Ref("owner")` удален метод `Unique()`.
- В `schema/petanalysis.go` для `field.String("pet_id")` добавлен метод `Required()`.
- В `schema/petanalysis.go` для `field.Time("analysis_date")` добавлен метод `Nillable()`.
- Эти изменения позволяют одному питомцу иметь несколько записей об анализах


## [2.0.2] - 2026-01-13

### Добавлено
- Добавлена опция "none" в перечисление статусов питомца (pet status enum).
- Добавлено поле `pets` в ответ User и реализована поддержка предзагрузки (preload) для этого поля.

### Изменено
- Удалена опция "none" из перечисления репродуктивных статусов.
- Исправлен JSON-тег `treatment_id` в схеме Pet.
- Обновлена схема OpenAPI и TypeScript модели в соответствии с изменениями в перечислениях и добавлении поля `pets`.

### Технические детали
- Обновлены определения перечислений для статуса питомца и репродуктивного статуса.
- Скорректирован JSON-тег для поля `treatment_id` в модели Pet для корректной сериализации/десериализации.
- Добавлена логика для предзагрузки связанных питомцев при запросе информации о пользователе.
- Сгенерированы обновленные OpenAPI спецификации и TypeScript модели.


## [2.0.1] - 2026-01-12 - Фундаментальный Рефакторинг и Переход на Современный Стек (Супер-Релиз)

Этот релиз представляет собой **полную переработку и фундаментальный рефакторинг** всего бэкенда, закладывающий прочную основу для будущих проектов и обеспечивающий высокую производительность, надежность и удобство разработки. Проект теперь служит образцом для создания современных Go-бэкендов, готовых к масштабированию и легкой поддержке.

### Добавлено
- **Huma v2 (OpenAPI 3.1):** Полный переход на Huma для построения API, обеспечивающий автоматическую генерацию высококачественной документации OpenAPI 3.1, строгую валидацию запросов/ответов и улучшенную структуру API.
- **Стандартный `net/http`:** Использование нативного `net/http` mux для маршрутизации, что обеспечивает максимальную гибкость и производительность без зависимостей от тяжеловесных фреймворков.
- **Ent Framework:** Полный переход на Ent в качестве ORM, предоставляющий мощные возможности для работы с базой данных, включая:
    - **Схемы данных:** Все GORM модели переписаны на декларативные Ent схемы.
    - **Интерцепторы:** Внедрение кастомных интерцепторов Ent для расширения функциональности ORM (например, для автоматического управления полями `CreatedAt`/`UpdatedAt`).
    - **Миксины:** Использование миксинов Ent для реализации общих паттернов, таких как `Soft Delete`, что значительно упрощает управление жизненным циклом сущностей.
- **Единая система обработки ошибок (`apperrors`):** Разработан и интегрирован централизованный пакет `apperrors` для унификации всех ошибок приложения, обеспечивающий консистентные ответы API и легкую диагностику.
- **`Request ID` для отслеживания:** Внедрение middleware для автоматического добавления уникального `Request ID` к каждому запросу, что значительно упрощает отслеживание запросов через логи и мониторинг.
- **Интеграция `charmbracelet/log` поверх стандартного `slog`:** Переход от тяжеловесных логгеров (например, Zap) к стандартному `slog` с красивой и функциональной оберткой `charmbracelet/log`, что обеспечивает улучшенное структурированное логирование с поддержкой различных форматов (текст, JSON) и уровней детализации при минимальных зависимостях.

### Изменено
- **Архитектурный Рефакторинг:** Полная переработка архитектуры бэкенда с переходом от Echo к Huma и от GORM к Ent, а также **отказ от избыточных внешних зависимостей в пользу стандартных библиотек Go**, что привело к значительному улучшению модульности, чистоты кода и производительности.
- **Переписанные Хендлеры:** Все HTTP-обработчики были полностью переписаны с учетом новой архитектуры Huma и `net/http`, обеспечивая более чистый и эффективный код.
- **Обновленная Структура DTO:** Структуры Data Transfer Objects (DTO) были переработаны для лучшей ясности, консистентности и соответствия новым схемам Ent.
- **Улучшенное Логирование:** Оптимизировано логирование во всех слоях приложения, включая middleware, сервисы и обработчики, для предоставления более полной и полезной информации.
- **Рефакторинг `Soft Delete`:** Реализация мягкого удаления теперь полностью основана на миксинах и интерцепторах Ent, обеспечивая надежное и легко расширяемое решение.
- **Удаление Устаревших Компонентов:** Полностью удалены все зависимости и код, связанные с Echo, GORM и старой документацией Swagger.

### Удалено
- Фреймворк Echo и все связанные с ним зависимости.
- ORM GORM и все связанные с ним зависимости.
- Старая документация Swagger и связанные генераторы.
- Микросервис `blood-microservice` (его функционал был интегрирован в основной бэкенд для упрощения архитектуры монорепозитория).
- **Тяжеловесные логгеры (например, Zap) и другие избыточные внешние библиотеки**, замененные на стандартные решения Go.

### Технические детали
- **Обновленные Зависимости Go:** Все зависимости проекта были обновлены до последних стабильных версий.
- **Оптимизация Зависимостей:** Принято стратегическое решение о **минимизации внешних зависимостей и максимальном использовании стандартных библиотек Go**, что повышает стабильность, безопасность и упрощает поддержку проекта.
- **Реорганизация `internal/`:** Проведена значительная реорганизация структуры каталога `internal/` для повышения модульности, четкого разделения ответственности и улучшения навигации по коду.
- **Улучшенная Валидация:** Внедрены более строгие механизмы валидации данных на уровне API и ORM.
- **Подготовка к Будущему:** Текущая архитектура обеспечивает высокую гибкость и готовность к дальнейшему развитию, позволяя легко добавлять новые функции и сервисы.


## [1.3.5] - 2025-12-31

### Добавлено
- Добавлено поле BirthDate для ввода точной даты рождения питомца. Система автоматически вычисляет возраст в годах и месяцах или дату рождения на основе введённых данных.

### Технические детали
- Обновлена модель Pet, структуры PetCreate и PetUpdate для поддержки BirthDate.
- Добавлена функция calculateAgeFields в pet_service.go для синхронизации возраста и даты рождения.

## [1.3.4] - 2025-12-31

### Технические детали
- Добавлен размер поля ID в моделях Pet и User для оптимизации схемы БД.
- Скрыты внутренние поля (CreatedAt, UpdatedAt, ID) из Swagger-документации в вложенных структурах Pet для улучшения читаемости API.
- Выделены хендлеры пула запросов крови в отдельный BloodRequestHandler и вынесены маршруты в группу /blood-request для лучшей организации кода.

## [1.3.3] - 2026-12-31

### Изменено
- Обновлена длина полей ID в моделях Pet и User до 15 символов
- Изменен формат генерации ID пользователя: теперь используются шестизначные порядковые номера
- Рефакторинг ролей пользователей: удалена роль `UserRoleClinic` и связанная с ней логика локализации

### Технические детали
- Скорректированы ограничения длины строк для идентификаторов в базе данных
- Обновлена логика формирования кастомных последовательностей для ID пользователей
- Очищены перечисления (enums) и маппинги переводов от устаревшей роли клиники

## [1.3.2] - 2025-12-31

### Добавлено
- Новые методы в PetRepository, PetService и PetHandler для полной поддержки расширенной модели питомца
- Интеграция библиотеки mapstructure для более надежной конвертации DTO в доменные модели

### Изменено
- Рефакторинг логики управления данными питомцев для повышения производительности
- Проведен небольшой рефакторинг структуры обработчиков и сервисного слоя

### Исправлено
- Исправлены маршруты для загрузки аватаров, обеспечивающие корректную обработку файлов

### Технические детали
- Обновлены сигнатуры методов в слое репозитория для работы с вложенными структурами здоровья и анализов
- Оптимизирована работа с маппингом данных через mapstructure

## [1.3.1] - 2025-12-30

### Добавлено
- Новая модель PetBonus для отслеживания специальных атрибутов и достижений животных
- Поле bonuses в основную структуру Pet для интеграции системы вознаграждений

### Изменено
- Реорганизация кода: методы UserPrefix сгруппированы непосредственно с определением типа для улучшения навигации
- Обновление логики сериализации бонусов питомцев

### Технические детали
- Добавлена структура PetBonus с поддержкой GORM
- Проведен рефакторинг методов UserPrefix без изменения функциональности

## [1.3.0] - 2025-12-30

### Добавлено
- Расширенная модель Pet с вложенными структурами: PetHealth, PetTreatments и PetAnalysis
- Детальное отслеживание здоровья: репродуктивный статус, история приема лекарств, хирургические вмешательства и записи о переливаниях
- Логирование процедур: поля для вакцинации (бешенство/инфекции), дегельминтизации и обработки от эктопаразитов
- Система анализов: поддержка различных лабораторных тестов (FeLV, FIV, бабезиоз и др.) с указанием метода тестирования (ПЦР, ИФА, микроскопия)

### Удалено
- Удалены устаревшие обработчики и модели ветеринарных клиник и запасов крови
- Удалены неиспользуемые статусы донаций, фильтры поиска и интерфейсы репозиториев клиник
- Очищены избыточные перечисления (enums) и устаревшие поля в сущностях User и Pet

### Изменено
- Рефакторинг логики генерации ID для питомцев с использованием кастомных префиксов и последовательностей БД
- Стандартизация структуры проекта и улучшение читаемости кода через аннотации и комментарии к хукам GORM

### Технические детали
- Проведена масштабная реорганизация основных структур данных для поддержки ветеринарных записей
- Оптимизация архитектуры бэкенда за счет удаления технического долга и неиспользуемого кода

## [1.2.5] - 2025-12-17

### Изменено
- Обновлены строки локализации для условий содержания и состояния здоровья
- Значение `outdoor` в перечислении LivingCondition заменено на `self_outdoor`
- Обновлены переводы на русский язык для справочников условий содержания и здоровья

### Технические детали
- Проведен рефакторинг констант LivingCondition в коде и базе данных
- Обновлены маппинги локализации для корректного отображения статусов в UI

## [1.2.4] - 2025-12-17

### Добавлено
- Новые эндпоинты справочных данных для состояния здоровья и репродуктивного статуса
- Поддержка новых типов перечислений (enum) для здоровья и репродукции в API

### Изменено
- Обновлены значения перечисления для условий содержания (living conditions)
- Упрощена валидация данных за счет удаления избыточных преобразований типов

### Технические детали
- Реализованы обработчики для эндпоинтов справочников здоровья и репродукции
- Оптимизирована логика проверки типов в сервисном слое
- Обновлена документация Swagger для новых справочных эндпоинтов

## [1.2.3] - 2025-12-17

### Удалено
- Удалены эндпоинты API для управления запасами крови (Blood Stocks)
- Удалены соответствующие методы из репозиториев и сервисного слоя

### Изменено
- Обновлены ссылки на структуры ответов с ошибками для обеспечения консистентности API
- Рефакторинг обработки исключений в контроллерах справочных данных

### Технические детали
- Коммит: d4e5f6a - Remove blood stocks API and update error references
- Очистка неиспользуемых DTO и моделей, связанных с BloodStock
- Обновлена документация Swagger: удалены маршруты /blood-stocks

## [1.2.2] - 2025-12-17

### Изменено
- Рефакторинг: перенос обработчиков BloodStock и VetClinic в пакет alpha
- Обновление импортов и ссылок в соответствии с новой структурой пакетов
- Переименование BloodRepository в bloodinfo_repository для большей ясности
- Обновление тегов Swagger для лучшей организации документации

### Добавлено
- Добавлены новые группы крови для кошек в сиды (seeds)

### Исправлено
- Исправлено описание маршрута для подтверждения загрузки аватара

### Удалено
- Удален неиспользуемый файл тестов хранилища S3
- Закомментированы неиспользуемые эндпоинты статусов

### Технические детали
- Коммит: a2b4c8d - Refactor handlers into alpha package and update blood info
- Проведена реорганизация структуры проекта для выделения альфа-функционала
- Обновлены зависимости в main.go и роутере для новых путей пакетов

## [1.2.101] - 2025-12-13

### Изменено
- Переход с BloodSearch API на BloodRequest API
- Переименование proto-пакета с bloodsearch на bloodrequest
- Обновление типов клиентов, обработчиков и названий сервисов
- Переименование PetRow в BloodRequest в моделях и репозиториях
- Обновление миграций; удаление модели BloodSearch из бэкенда

### Технические детали
- Коммит: 9468f08 - Switch to BloodRequest API and remove BloodSearch
- Обновлены все импорты и сгенерирован новый proto/connect код
- Изменены поля DTO: bloodGroup на []string, bloodComponents на []int32, добавлено description

## [1.2.100] - 2025-12-13

### Добавлено
- Добавлен petStatus в модель Pet (PetRole) и DTO; поддержка обновлений
- Валидация enum-значений с сохранением оригинальных значений

### Изменено
- Обновление Swagger: теги, типы запросов/ответов; переименование godoc
- Переименование blood_search_service.go в blood_request_service.go
- Обновление примеров крови и названий компонентов плазмы в seeds
- Комментирование миграций для VetClinic, BloodStock, Donation

### Технические детали
- Коммит: 6fc10f7 - Add PetStatus; refine validation, docs, seeds
- Добавлена поддержка статуса питомца в сервисах и обработчиках

## [1.2.0] - 2025-12-13

### Исправлено
- Исправление путей эндпоинтов и маппинга обработчиков для BloodRequest Pool
- Обновление main.go и pet_handler.go для корректной работы с новыми путями

### Технические детали
- Коммит: 5e46542 - Fix blood request pool endpoint paths and handler mapping
- Исправлены пути подключения и обработчики для микросервиса крови

## [1.1.103] - 2025-12-12

### Изменено
- Встраивание сборки фронтенда в Docker-образ; удаление монтирования томов
- Обновление docker-compose.yml и frontend/Dockerfile

### Технические детали
- Коммит: c987617 - Embed frontend build into image; drop mount
- Улучшение процесса деплоя и уменьшение зависимостей от томов

## [1.1.102] - 2025-11-25

### Исправлено
- Использование явного WHERE clause в запросах GORM для предотвращения потенциальных проблем
- Исправлена проверка валидации в методе Update для требования непустого ID
- Поддержание консистентных паттернов обработки ошибок в методах репозитория

## [1.1.101] - 2025-11-24

### Изменено
- Тип ID питомца изменен с целочисленного на строковый формат
- Добавлена генерация ID питомцев в пользовательском формате последовательности
- Обновлена документация API для отражения строкового типа ID
- Обновлены интерфейсы кэша и репозиториев для работы со строковыми ID
- Добавлен плавный переход на PG при недоступности Redis кэша
- Версия API увеличена до 1.1.0, обновлено название сервиса

## [0.1.200] - 2025-11-21

### Изменено
- Использование Bun Alpine image и реорганизация Dockerfile бота для улучшения кэширования сборки.
- Обновление Dockerfile: копирование только необходимых файлов вместо всей директории, что ускоряет билд.
- Временное изменение: копирование всей директории `bot` в некоторых вариантах сборки для корректности (см. коммиты).
- Оптимизация Docker-сборок и исправления TypeScript-типов для бота.
- Добавлен `Taskfile.yaml` в корне проекта для автоматизации задач; рефакторинг процесса сборки.

### Технические детали
- Коммиты (последние 5): `e68f783`, `8c60c6e`, `93a6949`, `6aea35f`, `45ca19e` — правки Dockerfile/CI и добавление Taskfile для автоматизации.
- Эти изменения направлены на ускорение и стабилизацию сборки бота и улучшение процесса автоматизации разработки.

## [0.1.102] - 2025-11-07

### Добавлено
- Эндпоинт разработки /dev/restore-user/{id} для восстановления мягко удаленных пользователей
- Эндпоинт разработки /dev/reset-user/{id} для сброса пользователя в начальное состояние
- Эндпоинт разработки /dev/deleted-users для получения списка мягко удаленных пользователей
- Метод RestoreUser в репозитории пользователей для восстановления удаленных записей
- Метод GetDeletedUsers в репозитории пользователей для получения списка удаленных пользователей
- Функциональность сброса пользователя в начальное состояние для целей разработки и тестирования

### Технические детали
- Реализован эндпоинт /dev/restore-user/{id} для восстановления мягко удаленных пользователей
- Добавлен эндпоинт /dev/reset-user/{id} для сброса всех полей пользователя к значениям по умолчанию
- Создан эндпоинт /dev/deleted-users для получения списка всех мягко удаленных пользователей
- Внедрен метод RestoreUser в UserRepository для восстановления удаленных записей в базе данных
- Добавлен метод GetDeletedUsers в UserRepository для получения списка удаленных пользователей
- Реализована валидация существования пользователя перед выполнением операций восстановления и сброса
- Обновлена документация API с описанием новых эндпоинтов разработки для управления пользователями
- Добавлена соответствующая обработка ошибок для случаев, когда пользователь не найден
- Интегрированы новые эндпоинты в систему маршрутизации с префиксом /dev
- Реализована логика сброса пользователя в начальное состояние с очисткой всех полей кроме идентификатора

## [0.1.101] - 2025-11-07

### Добавлено
- Эндпоинт разработки /dev/reset-user/{id} для сброса email и телефона пользователя
- Метод ResetUser в репозитории пользователей для очистки полей email и phone
- Функциональность сброса данных пользователя для целей разработки и тестирования

### Технические детали
- Реализован эндпоинт /dev/reset-user/{id} для сброса email и телефона пользователя до пустых строк
- Добавлен метод ResetUser в UserRepository для выполнения операции сброса в базе данных
- Внедрена валидация существования пользователя перед выполнением сброса
- Обновлена документация API с описанием нового эндпоинта разработки
- Добавлена соответствующая обработка ошибок для случаев, когда пользователь не найден
- Реализована интеграция эндпоинта в систему маршрутизации с префиксом /dev

## [0.1.1] - 2025-11-01

### Добавлено
- Redis кэширующий слой с поддержкой JSON сериализации
- Обертки кэширующих репозиториев для справочных данных групп крови и компонентов
- Отдельный пакет для конфигурации сервера и graceful shutdown
- Выделенный пакет миграций для управления схемой базы данных
- Интеграция Redis клиента и валидатора в зависимости проекта

### Изменено
- Рефакторинг конфигурации сервера с выделением в отдельный пакет
- Переработана система graceful shutdown с улучшенной обработкой зависимостей
- Обновлена структура миграций для лучшей модульности и поддерживаемости

### Технические детали
- Реализован Redis кэширующий слой с JSON сериализацией для справочных данных
- Добавлены кэширующие обертки для репозиториев BloodGroupRepository и BloodComponentRepository
- Создан отдельный пакет server для конфигурации и управления жизненным циклом сервера
- Выделен пакет migrations для управления миграциями базы данных с улучшенной структурой
- Обновлены зависимости проекта для включения Redis клиента и валидатора
- Реализована стратегия кэширования с TTL для справочных данных о крови
- Добавлена обработка ошибок кэширования с fallback на прямой запрос к базе данных
- Улучшена конфигурация сервера с поддержкой параметров Redis и таймаутов
- Обновлена система graceful shutdown с корректным закрытием соединений с Redis и базой данных
- Рефакторинг main.go с использованием нового пакета конфигурации сервера
- Добавлены тесты для кэширующих репозиториев и проверки корректности данных
- Обновлена документация с описанием новой архитектуры кэширования и конфигурации

## [0.1.0] - 2025-10-30

### Добавлено
- Система управления компонентами крови с переименованием BloodType в BloodComponent
- Эндпоинт групп крови с параметром типа животного
- Эндпоинт компонентов крови для получения всех доступных компонентов
- Система сидинга базы данных для предварительного заполнения данных о крови
- Обновленная обработка ошибок "не найдено" в сервисах

### Удалено
- Сервис локаций и связанные CRUD операции
- Неиспользуемые методы из интерфейса LocationRepository

### Изменено
- Упрощена обработка локаций через прямое использование репозитория в обработчике справочных данных
- Обновлена документация API для новых эндпоинтов крови
- Рефакторинг зависимостей в main.go после удаления LocationService

### Технические детали
- Переименованы модели BloodType в BloodComponent и BloodGroup для лучшей семантики
- Реализован эндпоинт /api/reference/blood-groups с параметром pet_type для фильтрации групп крови
- Добавлен эндпоинт /api/reference/blood-components для получения всех компонентов крови
- Внедрена система сидинга базы данных с предварительным заполнением справочных данных о крови
- Обновлена обработка ошибок в сервисах для корректного возврата статуса 404 при отсутствии данных
- Удален LocationService и заменен прямым использованием LocationRepository в reference handler
- Упрощен интерфейс LocationRepository путем удаления неиспользуемых CRUD методов
- Обновлена документация Swagger с описанием новых эндпоинтов крови и их параметров
- Рефакторинг инъекции зависимостей в main.go после удаления сервиса локаций
- Добавлены соответствующие модели запросов и ответов для операций с группами крови и компонентами
- Реализована валидация параметров запроса для эндпоинтов крови

## [0.0.7] - 2025-10-28

### Добавлено
- Система управления локациями с полным CRUD функционалом
- Репозиторий локаций с PostgreSQL бэкендом для операций создания, чтения, обновления и удаления
- Сервис локаций с бизнес-логикой и валидацией данных
- REST API обработчики для эндпоинтов локаций
- Интеграция локаций в систему справочных данных
- Эндпоинт локаций в маршрутах справочных данных

### Удалено
- Устаревший JavaScript бэкенд код для управления локациями

### Технические детали
- Реализован полный CRUD функционал для управления локациями
- Создан репозиторий локаций с PostgreSQL реализацией для хранения данных
- Разработан сервисный слой с бизнес-логикой и валидацией данных локаций
- Добавлены REST API обработчики для всех операций с локациями
- Интегрированы локации в систему справочных данных приложения
- Добавлен эндпоинт локаций в маршруты справочных данных
- Удален устаревший JavaScript бэкенд код, замененный на Go реализацию
- Обновлена документация Swagger с описанием новых эндпоинтов локаций
- Добавлены соответствующие модели запросов и ответов для операций с локациями
- Реализована валидация входных данных для создания и обновления локаций

## [0.0.6] - 2025-10-25

### Добавлено
- Поля OnBoarding и AllowGeo в модель User для управления процессом адаптации и геолокацией
- Обновление сервиса UserUpdate для поддержки поля AllowGeo вместо OrganizationName
- Автомиграция для новых полей модели User

### Изменено
- Обновлена документация Swagger с описанием новых полей OnBoarding и AllowGeo
- Изменен корневой обработчик для возврата HTML ответа со стилизованной ссылкой
- Удалено устаревшее поле OrganizationName из операций обновления пользователя

### Технические детали
- Добавлены поля OnBoarding (boolean) и AllowGeo (boolean) в модель User
- Реализована автомиграция для применения изменений структуры таблицы пользователей
- Обновлен сервис UserUpdate для работы с полем AllowGeo
- Расширены модели Swagger для отражения новых полей в API документации
- Корневой эндпоинт теперь возвращает HTML с стилизованной ссылкой вместо JSON ответа
- Удалена поддержка поля OrganizationName в операциях обновления пользователя

## [0.0.5] - 2025-10-24

### Добавлено
- Эндпоинты управления ветеринарными клиниками с полным CRUD функционалом
- Репозиторий ветеринарных клиник с операциями создания, чтения, обновления и удаления
- Сервисный слой для бизнес-логики управления клиниками
- REST обработчики для маршрутов ветеринарных клиник
- Автомиграция для таблиц базы данных ветеринарных клиник
- Обновленная документация API для новых эндпоинтов клиник

### Изменено
- Рефакторинг импортов репозиториев для использования пакета интерфейсов
- Обновлена структура зависимостей для поддержки модуля ветеринарных клиник
- Добавлена поддержка мягкого удаления для моделей Pet и VetClinic
- Обновлена модель User для использования указателя на поле DeletedAt для консистентности

### Технические детали
- Реализован полный CRUD функционал для управления ветеринарными клиниками
- Добавлены эндпоинты для создания, получения, обновления и удаления клиник
- Внедрена бизнес-логика валидации и обработки данных клиник
- Обновлена система автомиграций для создания таблиц клиник
- Рефакторинг импортов репозиториев для улучшения модульности кода
- Расширена документация Swagger с описанием всех новых эндпоинтов клиник
- Добавлены соответствующие модели запросов и ответов для операций с клиниками
- Заменены жестко заданные сообщения об ошибках на фактические ошибки из сервисного слоя
- Сообщения об ошибках из сервисного слоя теперь корректно передаются в API ответы

## [0.0.4] - 2025-10-23

### Добавлено
- CRUD операции для питомцев с полной валидацией входных данных
- Эндпоинты справочных данных для перечислений и пород животных
- Механизм graceful shutdown с 30-секундным таймаутом
- Эндпоинт удаления пользователей (soft delete)
- Автомиграция для всех доменных моделей (пользователи, питомцы, справочники)
- Улучшенная обработка ошибок и консистентность логирования

### Изменено
- Обновлена документация Swagger для новых эндпоинтов питомцев и справочных данных
- Расширена система валидации для всех доменных объектов
- Улучшена структура логов для лучшей отладки и мониторинга

### Технические детали
- Реализованы полные CRUD операции для управления питомцами с валидацией
- Добавлены эндпоинты для получения справочных данных (типы животных, породы, статусы)
- Внедрен graceful shutdown для корректного завершения работы сервера
- Реализовано мягкое удаление пользователей с сохранением истории
- Расширена система автомиграций для всех бизнес-сущностей
- Унифицирована обработка ошибок и формат логирования во всех компонентах
- Обновлены модели Swagger для всех новых эндпоинтов и операций

## [0.0.3] - 2025-10-22

### Добавлено
- Валидация входных данных в обработчиках пользователей с использованием пакета validator/v10
- Middleware валидации для эндпоинтов регистрации и обновления пользователей
- Структурированные ответы с ошибками валидации на русском языке
- Конфигурация сервера через переменные окружения SERVER_PORT и ENVIRONMENT

### Изменено
- Отключен mock middleware для аутентификации Telegram
- Обновлена инициализация логгера с передачей окружения

### Технические детали
- Интегрирован пакет validator/v10 для валидации запросов
- Добавлены правила валидации для данных пользователя при регистрации и обновлении
- Реализован middleware для обработки ошибок валидации с переводом на русский язык
- Настроено чтение порта сервера и окружения из переменных среды с значениями по умолчанию
- Убрана mock аутентификация

## [0.0.2] - 2025-10-22

### Добавлено
- Подключение к базе данных PostgreSQL с настройкой пула соединений
- Автомиграция для модели User при запуске приложения
- Репозиторий пользователей с PostgreSQL реализацией
- Сервис пользователей с операциями регистрации, обновления и управления профилем
- Mock middleware для аутентификации Telegram в режиме разработки
- Реструктуризация маршрутов пользователей с эндпоинтами регистрации, профиля и поиска по Telegram
- Обновление документации Swagger с новыми эндпоинтами и моделями запросов/ответов

### Технические детали
- Реализована автоматическая миграция структуры таблицы пользователей
- Добавлены операции CRUD для управления пользователями через репозиторий
- Создан сервисный слой для бизнес-логики пользователей
- Mock аутентификация эмулирует Telegram WebApp данные для тестирования
- Добавлены валидации для данных пользователя при регистрации и обновлении
- Обновлены модели Swagger для всех новых эндпоинтов



## [0.0.1] - 2025-10-20

### Добавлено
- Базовая настройка API сервера с использованием Fiber v2
- Интеграция Swagger для документирования API
- Middleware CORS, настроенный для Telegram MiniApp
- Middleware структурированного логирования запросов
- Обработчики для корневого эндпоинта и пользовательских запросов
- Тестовые модели данных User и Animal
- Генерация документации Swagger в форматах JSON, YAML и Go
- Инициализация модульных зависимостей для Swagger и Fiber v2

### Изменено
- Обновление до Fiber версии v2
- Реструктуризация проекта для лучшей организации кода

### Технические детали
- Настроен CORS для разрешения запросов из Telegram MiniApp
- Реализовано логирование в структурированном формате (JSON)
- Добавлены примеры запросов и ответов в Swagger документации
- Созданы базовые модели данных с валидацией

---
